# Companion Space v0.1 Manual Matrix

- Initial verification date: `2026-08-05`
- Latest evidence update: `2026-08-09`

Status meanings:

- `PASS`: completed with executable evidence.
- `PARTIAL`: an automated portion passed, but the named manual/hardware portion did
  not run.
- `WAITING`: blocked on a required local runtime.
- `NOT RUN`: no valid execution evidence was collected.
- `N/A`: outside the v0.1 scope.

## Provider Matrix

| Scenario | Setup | Expected result | Status | Evidence / gap |
| --- | --- | --- | --- | --- |
| Mock-only fresh state | No API key, built-in Mock provider | Complete space, material, character, call, citation, recap, memory, and review flow without external services | PASS | `npm run test:e2e`: Playwright Chromium `14 passed, 1 skipped (2.2m)` from an isolated non-iCloud checkout of the same worktree. The full-suite journey completed in `1.1m` (focused rerun `54.3s`) and asserted blank-space setup, note ingestion, citations, real turn-path board actions, Mermaid corrupt fallback, recap, memory confirmation, review rescheduling, and required character option counts. Realtime guardrails also covered `session.open` microphone gating, PTT interruption/commit ordering, avatar states, and persisted metric readback. |
| OpenAI-compatible text chat | Custom Base URL + real key | Stream text and surface the public provider error on failure | NOT RUN | No real endpoint or key was supplied. Automated browser stubs did verify visible/retryable 401, 429, and post-delta disconnect behavior without vendor fallback. Custom non-Ollama hostnames fail closed when DNS cannot resolve; each operation now pins TCP to its validated DNS snapshot while retaining the original hostname for TLS/SNI/Host. Real credentials are still required before this row can become PASS. |
| Anthropic text chat | Real Anthropic key | Stream text and expose provider-specific failures | NOT RUN | No real key was supplied. |
| Gemini text/chat and embeddings | Real Gemini key | Chat and embedding-backed retrieval work when explicitly assigned | NOT RUN | No real key was supplied. |
| ElevenLabs TTS | Real ElevenLabs key and Voice ID | Voice output works or fails with a visible provider error | NOT RUN | No real key or voice was supplied. |
| Ollama local setup | Ollama `0.32.5` with installed `deepseek-r1:1.5b` | Local chat works through the explicit configured connection | PASS | An isolated Companion vault/space created an empty-Key Ollama connection, discovered the model, bound `chat_llm`, and received streamed delta plus final `CompanionTurn`; temporary storage was removed and the master password was not present in persisted bytes. |

## Desktop And Audio Matrix

| Scenario | Setup | Expected result | Status | Evidence / gap |
| --- | --- | --- | --- | --- |
| Desktop Chromium, text-first | Playwright `Desktop Chrome` profile | Mock product loop and call shell work | PASS | Automated journey passed. This is browser state-machine evidence only; the PCM injection path does not prove real microphone capture. |
| Desktop Chrome microphone and playback | Current Chrome plus physical mic/speakers | Capture, playback, VAD, and interruption remain stable | NOT RUN | No physical audio-device session was executed. |
| Desktop Edge | Current Edge plus physical mic/speakers | Same supported behavior as Chrome | NOT RUN | No Edge session was executed. |
| Bluetooth headset | Desktop browser plus headset | Input/output routing remains stable during a call | NOT RUN | No headset session was executed. |
| Mobile browser over LAN | Future trusted-device flow | Not applicable to v0.1 | N/A | Phone pairing and LAN access are v0.2. |

## Character Asset Matrix

| Scenario | Setup | Expected result | Status | Evidence / gap |
| --- | --- | --- | --- | --- |
| Licensed standard VRM 1.0 | Headed desktop Chromium with hardware WebGL | Avatar loads, animates, rejects software rendering, and sustains median ≥30 FPS | PASS | `npm run test:e2e:vrm-performance`: `1 passed (33.7s)` using the bundled licensed VRM. |
| Damaged or invalid VRM | Corrupt `.vrm` upload plus forced browser load failure | Load failure is visible and the call remains usable through fallback UI | PASS | An isolated API upload returned HTTP 400 with an actionable format/signature error; the default Playwright guardrail kept captions, interruption, session ending, and text conversation usable after VRM load failure. |
| WebGL unavailable | Browser with all WebGL canvas contexts disabled | The character surface falls back without blocking text conversation | PASS | Playwright forced `webgl`, `webgl2`, and `experimental-webgl` contexts to return null, then verified the explicit WebGL error, 2D portrait, WebSocket connection, and text reply. |
| Session Review reference-image comparison | Defined visual reference plus desktop viewport | Compare layout, hierarchy, spacing, and typography against the approved reference | N/A | v0.1 defines no reference image; no visual-fidelity score is claimed. Functional and accessible semantics passed E2E. |
| Repository-owned four-state VRMA set | Bundled `idle`, `listening`, `thinking`, and `speaking` assets | Every motion is reproducible, redistributable, and structurally valid | PASS | The four project-generated assets are dedicated under CC0 1.0 Universal. `node scripts/generate-cc0-vrma.mjs --check` passed; Khronos `gltf-validator` `2.0.0-dev.3.10` reported zero errors and zero warnings for every file. |
| VRMA cross-version runtime binding | Four bundled motions loaded and retargeted against one VRM 1.0 model and one VRM 0.x model | Each exact combination creates a playable clip without unresolved bindings | PASS | `node scripts/verify-cc0-vrma-runtime.mjs` passed all eight combinations. Each clip contained five tracks, had zero unresolved bindings, remained running after update, and changed the normalized hips quaternion. |
| `Stage Companion` four-state motion | Isolated real Chromium session using the bundled manifest | All four avatar states use the VRMA runtime and visibly advance | PASS | For `idle`, `listening`, `thinking`, and `speaking`, diagnostics reported `mode=vrma`, `running=true`, and `weight=1.000`; animation time advanced and the skeleton changed. |
| VRMA motion policy and reduced motion | Playwright `1.62.1`, bundled motion, reduced-motion preference, external and Blob URLs | Preserve runtime availability under reduced motion, reject untrusted URLs, and allow only manifest-authorized Blob loading | PASS | Official automation completed `5 passed (55.4s)`, covering the full Mock journey, bundled VRM, reduced motion, an untrusted external URL, and a manifest-authorized temporary Blob URL. Report: `D:\Codex\Companion-Space-playwright-20260809\playwright-report\index.html`. |
| AIRI v0.11.3 card-only/Live2D/Spine ZIP | Clean-room fixture pinned to AIRI commit `dbf8124`, with no model or an unsupported display-model format | Import only bounded persona data; never read, persist, activate, export, serve, or request the unsupported model | PASS | Focused backend `118 passed`, full backend `440 passed`, Ruff, TypeScript, ESLint, Next build, and isolated Chromium `1 passed (40.2s)`. Tests cover card-only plus the fixed-contract Live2D/Spine formats, prompt/extension/unknown-field removal, declared-model asset 404, built-in VRM fallback, portable Windows ZIP names/collisions, and unchanged space activation. This is schema-contract evidence from a synthetic source-pinned fixture, not a claim that the AIRI exporter itself was executed. |
| AIRI v0.11.3 embedded VRM ZIP | Source-pinned clean-room package containing a repository-authorized real VRM | Validate one declared VRM, derive rights from its metadata, serve it only through the protected asset API, and require explicit activation | PASS | Focused backend `122 passed`, full backend `444 passed`, Ruff, TypeScript, ESLint, Next build, and isolated Chromium Mock journey `3 passed (3.2m)`. The browser proved exact protected model bytes, Blob-backed VRM 0.x rendering rather than the default VRM 1.0 fallback, retained persona, zero AIRI-source/built-in fallback requests, and unchanged space defaults. |
| Managed per-state VRMA overlays | Owner-uploaded `idle/listening/thinking/speaking` VRMA, protected asset API, existing character recipe | Override one runtime state without mutating the recipe; remove it to reveal the prior fallback; prevent unlicensed export | PASS | Focused backend `27 passed`, full backend `479 passed`, Ruff, TypeScript, ESLint, Next build, two independent `CLEAR` reviews, and isolated Chromium `1 passed (23.7s)`. The browser started at `0/0` procedural, loaded only the exact authenticated content-addressed overlay as `model/gltf-binary`, observed a running animated VRMA, then removed it, received 404 for the old asset, returned to procedural mode, and restored export. |
| Pointer/touch avatar gaze | Real WebGL VRM, WebGL-disabled 2D fallback, mouse/touch/blur/cancel paths, reduced-motion preference | Follow primary pointer input without remounting the runtime or restarting VRMA; reset safely; stay static under reduced motion | PASS | Next `15.5.22` production build, TypeScript, zero-warning ESLint, three new Chromium tests `3 passed (15.6s)`, and three impacted interaction/reduced-motion regressions `3 passed (19.0s)`. The tests read actual post-update LookAt yaw/pitch, lock the VRM scene-root instance, VRMA activation/time, Canvas identity and asset-request counts, and verify 2D computed eye translation plus reduced-motion stability. |

## Product-Flow Matrix

| Scenario | Setup | Expected result | Status | Evidence |
| --- | --- | --- | --- | --- |
| Blank space creation | Isolated Mock runtime | Space starts blank under the neutral `Companion Space` product | PASS | Playwright journey plus successful production build. |
| `law-luze` demo pack boundary | Fresh backend storage | Optional pack exists but is not auto-loaded | PASS | Backend regression `test_fresh_container_starts_blank_without_law_luze_seed`. |
| Scoped material retrieval and citation | Space with pasted note | Answer cites the server-retrieved material from the same space | PASS | Playwright journey asserted the note title in the assistant citation. |
| Board and demo playback | Mock space with indexed note | Board renders; demo can play, pause, advance, and accept a question | PASS | Playwright exercised the three-step demo and separately asserted board actions emitted by an actual realtime WebSocket turn. |
| Realtime interruption | Active Mock session | Playback stops within 250 ms and turn transitions cleanly after interruption | PASS | Default Playwright guardrail plus backend interruption/latency regressions passed. |
| Realtime scheduled playback | Steady-state bundled VRM plus fake-microphone VAD and Mock STT/LLM/TTS | The first Web Audio source is scheduled within 800 ms of `user.commit`, and the metric is accepted and persisted | PASS | Three independent serial browser samples were `546 ms`, `494 ms`, and `609 ms`; scheduled start is the browser proxy for first playback because Web Audio has no reliable hardware-audible callback, while the backend voice-path regression independently requires first binary audio below 800 ms. |
| Recap persistence | Ended Mock session | Generated recap becomes ready; edits persist and AI draft can be restored | PASS | Playwright journey navigated away/back and verified saved values. |
| Memory confirmation and review editing | Ended Mock session | Memory remains pending until explicit confirmation; prompt and answer edits persist | PASS | Playwright journey plus backend sensitive-memory regression. |
| 30-minute realtime stability | One production Mock WebSocket session | No state corruption or sustained browser/API memory growth | PASS | `npm run test:e2e:realtime-soak`: `1 passed (30.2m)`; soak timing recorded in `soak-evidence.json` was `30.1m` with 121 turn iterations and 61 samples. `residueFindings=[]`, `browserLeak=false`, `apiLeak=false`, `browserProcessLeak=false`, browser heap max delta was about `0.854 MiB`, and API/browser-process RSS stayed below baseline. |
| Raw-audio retention default | Completed audio soak | No raw audio remains in files, SQLite/WAL, or logs | PASS | Zero findings for audio extensions, exact uplink/downlink PCM canaries, RIFF/WAVE/audio/PCM markers, and transcript text after the 30-minute run. The standard backend suite now also recursively scans every isolated storage file and captured log for an exact PCM/RIFF/WAVE canary. |

## Infrastructure Matrix

| Scenario | Setup | Expected result | Status | Evidence / gap |
| --- | --- | --- | --- | --- |
| Compose configuration | Docker Compose CLI | Merged configuration parses successfully | PASS | `docker compose config --quiet` passed. |
| HTTPS full-stack smoke | Docker Desktop daemon plus `scripts/docker-smoke.sh` | Caddy, API, web, health route, app shell, vault proxy, and WebSocket guard pass | PASS | Isolated Compose smoke passed under project `companion-space-smoke-codex-0805-final`; a follow-up process listing was empty. Docker Engine `29.6.1` used the script's exact-error BuildKit compatibility fallback. |
| Four-state VRMA formal Docker deployment | Commit `a84c18a`, formal Windows stack, verified full backup | Build, deploy, and complete post-deployment smoke for the current VRMA change | PASS | API/Web rebuilt and now run images `sha256:c7f46e42...` and `sha256:7de7bb53...` with Caddy; all three containers are running with zero restarts. TLS 1.3 and hostname validation passed against the Caddy root, `/healthz`, `/`, and Vault status returned 200, and the deployed idle VRMA matched its 3616-byte manifest payload and SHA-256. SQLite remained `integrity_check=ok` with zero foreign-key violations, business counts were preserved, six non-database Storage files matched the verified backup, and the old API/Web images remain tagged `pre-vrma-1887675`. |
| Character Card V2/V3 formal Docker deployment | Commit `bbc9b56`, isolated V3 Mock journey, verified full backup, formal Windows stack | Import bounded persona content without executing prompt overrides or downloading remote assets, then preserve Storage and HTTPS health through rollout | PASS | Focused `81 passed`, full backend `395 passed`, Ruff, TypeScript, ESLint, Next build, independent review, and Chromium Mock journey passed. Production API/Web run `sha256:fba9fd28...` / `sha256:051640ea...` with zero restarts; HTTPS health/Web/Vault passed, SQLite integrity/FKs stayed clean, six non-database Storage hashes matched backup `companion-space-full-20260809-234250-aedfff7a`, and rollback tags `pre-character-card-033d13e` are present. |
| Local stage-background formal Docker deployment | Commit `67393f5` / formal `efec87a`, isolated Mock and fallback Chromium runs, verified full backup | Persist `neutral/study/midnight`, keep 3D and 2D rendering aligned, and preserve production data through rollout | PASS | Focused backend `87 passed`, full backend `409 passed`, Ruff, TypeScript, ESLint, Next build, Mock journey, exact WebGL fallback, and two independent reviews passed. Formal API/Web are `sha256:1b4427d...` / `sha256:094d5dc...`; HTTPS health/Web/Vault passed with zero restarts, 20 non-metric SQLite table hashes and six non-database files matched backup `companion-space-full-20260810-082027-dcde38bf`, rollback tags are present, and all project containers were stopped after verification. |
| AIRI v0.11.3 persona-only ZIP formal Docker deployment | Development `93daaf7` / formal `518c361`, source-pinned Chromium journey, verified full backup | Preserve the exact persona-only trust boundary and production Storage while deploying the fixed AIRI package contract | PASS | Focused backend `118 passed`, full backend `440 passed`, Ruff, TypeScript, ESLint, Next build, Chromium `1 passed (40.2s)`, and three independent reviews passed. Formal API/Web are `sha256:8ac6162...` / `sha256:01cd5e6...`; HTTPS Web/health/Vault passed with zero restarts, the authorized Caddy thumbprint matched all three locations, 20 non-metric tables and six non-database files matched backup `companion-space-full-20260810-110233-33eb7c73`, rollback tags are present, the temporary QA image was removed, all project services were stopped, and Docker Desktop was gracefully shut down. |
| Managed per-state VRMA formal Docker deployment | Development `9d0f1b9` / formal `f862205`, focused Chromium journey, verified full backup | Deploy trusted local overlays without mutating existing recipes or formal Storage | PASS | API/Web rebuilt as `sha256:2cd6d71e...` / `sha256:d8de09bb...`; HTTPS Web/health/Vault, OpenAPI operations, unauthenticated 401, certificate thumbprint, seven host/container hashes, zero restarts/OOM/errors, 20 non-metric table hashes, and six non-database file hashes passed against backup `companion-space-full-20260810-204136-00a671a8`. Rollback tags remain, exact QA images/caches were removed, formal services were stopped, and Docker Desktop was gracefully shut down. |
| Pointer/touch gaze formal Docker deployment | Development `f939c1f` / formal `f18a105`, isolated Chromium reports, verified full backup | Deploy clean-room stage gaze without changing formal Storage or leaving Docker running | PASS | Formal Web is `sha256:fa730fed...`; API/Caddy remained `sha256:2cd6d71e...` / `sha256:748016f2...`. HTTPS Web/health/Vault and HTTP redirect passed, three host/container source hashes matched, zero restarts/OOM/log errors were observed, 20 non-metric tables and six non-database files matched backup `companion-space-full-20260810-224957-b94e49b8`, rollback tag `pre-pointer-gaze-f18a105` remains, formal services were stopped, and Docker Desktop was gracefully shut down. |

## Windows Handoff Addendum — 2026-08-06

This addendum records new Windows evidence without replacing the 2026-08-05 release facts above.

| Scenario | Setup | Expected result | Status | Evidence / gap |
| --- | --- | --- | --- | --- |
| Native Windows shell/LF and backend gate | Windows PowerShell 5.1, native Python 3.12.13, repository checkout | Every tracked `.sh`/`.ps1` declares and contains LF; PowerShell scripts parse; Ruff and the full backend suite pass | PASS | The first shell smoke failed before `.gitattributes` existed. After the LF policy and smoke script were added, it reported 8 LF files and 3 parsed PowerShell files. Native Ruff passed; native Pytest passed `305 passed, 3 skipped, 7 warnings`. A Windows junction test covers the reparse-point rejection without administrator symlink privilege; three symlink-only cases skip when Windows Developer Mode is unavailable. After fast-forwarding `C:\Companion-Space`, the same smoke caught stale CRLF bytes left by the pre-existing checkout; exactly 8 shell files were normalized, the smoke passed, and Git remained clean. |
| GitHub-hosted Windows CI | `.github/workflows/ci.yml` `windows-smoke` job on `windows-latest` | Run PowerShell 5.1 shell/LF gate, Ruff, and the full backend suite | NOT RUN | The workflow is configured and parsed locally, but no hosted run URL exists in this local-only session. Do not infer hosted success from the native local PASS row. |
| Windows Mock product journey | Docker Desktop `4.85.0`, Engine `29.6.2`, isolated fresh Storage, in-app Chromium | Complete Vault, space, material, character, cited chat, demo, recap, memory, and review flow | PASS | Fresh isolated state completed the full UI journey. The indexed note produced a server citation; the three-step demo played, paused, advanced, rendered Mermaid, and accepted a question; recap edits, memory confirmation, and review edits persisted. No business console errors, raw-audio files, or plaintext test password were found. The isolated volume was removed after inspection. |
| Windows local HTTPS trust | Current-user Windows root store plus Caddy local CA | `https://companion.localhost` loads with the authorized Caddy root | PASS | Trusted root thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` was present; Caddy reported the root already trusted. Browser navigation and Vault status loaded without console warnings or errors. Windows `curl` required `--ssl-no-revoke` because a local CA has no revocation endpoint; certificate-chain validation remained enabled. |
| Portable material paths | Old production image, candidate image, isolated copy of production Storage | Old Mac/Windows/Docker absolute paths migrate atomically to Storage-relative POSIX keys | PASS | Backend suite covers all three legacy path styles, cross-root moves, identity mismatch, `..`, symlinks, idempotence, migration rollback, and delete rollback. A Docker rehearsal generated a real `/app/storage/...` row with the old image; the candidate rewrote it to `spaces/{space_id}/materials/{material_id}.md`, preserved exact content, returned zero updates on the second pass, and kept `PRAGMA integrity_check=ok`. |
| Windows production rollout | `C:\Companion-Space`, pre-update Storage backup, candidate API image | Existing data remains intact and HTTPS stack returns healthy | PASS | Seven Storage files (`212720` bytes) were copied and SHA-256 matched under `backups/pre-relative-path-20260806-173503`. API image `sha256:0fdcda3531cb504ac31b029ba4cb2ed6534fe8e3d7f1f6ce7b6eb38ae7ed7f04` is running; API/Web/Caddy are up, `/healthz` returns `{"ok":true}`, Vault reports initialized/locked, and production SQLite reports `integrity_check=ok` with no non-portable material paths. The previous API image remains tagged `pre-relative-path-ebdf693`. |
| Windows automated gates after hardening | Current Windows handoff worktree in Linux test/build containers | Backend and frontend release gates remain green | PASS | Full backend: Ruff plus `284 passed`; final affected-path rerun: Ruff plus `30 passed`. Frontend: TypeScript, ESLint, and Next `15.5.22` production build passed. |
| SQLite version and lock lifecycle | Python 3.13, version-0/version-2 fixtures, forced lock contention and late migration failure | Version 0 migrates atomically to 1; future versions fail closed; configured busy timeout applies before locks | PASS | Five pre-implementation failures proved the tests were live. Final full backend gate passed Ruff plus `297 passed`; the focused lifecycle/maintenance lane passed `13 passed`, including rollback, future-version race, and forced native-timeout-zero contention. |
| Windows online and full backup | PowerShell 5.1, Docker Desktop, running isolated API, SQLite WAL plus Storage tree | SQLite-only stays online and is marked incomplete; full mode checkpoints, snapshots, copies, hashes, and restores prior API state | PASS | Both modes ran successfully after real Windows execution caught and fixed two PowerShell-only defects. The full production pre-upgrade backup contains seven files (`220912` bytes), schema 0, clean integrity/FKs, and zero SHA-256 mismatches; the old API resumed before cutover. |
| SQLite production migration and restore rehearsal | Pre-upgrade full backup, candidate image, production Storage, separate restored copy | Production and restored copy reach schema 1 without logical or file loss | PASS | Production runs image `sha256:b18ede0b9f49ff338d06fb83f2ab11a9c1ff612d7cd33e2ff8bcf1728f75b49c`; `/healthz`, initialized/locked Vault, WAL, integrity, and foreign keys passed. All 15 business tables and 127 rows matched the schema-0 backup in both production and the restored copy; six non-database files had zero hash mismatches. Rollback image tag: `pre-sqlite-c778bd3`. |
| Learning-artifact restart recovery | Fresh service lifecycle, locked/uninitialized Vault, mixed runnable jobs, stale generated rows, exact production image, synthetic pending/running SQLite state | Startup recovers unfinished work once without blocking runnable jobs, duplicating rows, losing user decisions, or inventing errors | PASS | Commit `41817ef`; focused Ruff plus `11 passed`; final full Ruff plus `308 passed`. Exact image `sha256:30477ac621132add81a1bb3789eea27c784e6735df56f9d2aa372387273ef709` recovered an open pending and ended running session to ready with manual summary preserved, clean integrity/FKs, and no production Storage mount. Production backup `companion-space-full-20260808-234445-d74853ba` is verified; 14 non-metric tables and 81 row hashes match production exactly; API/Web/Caddy and trusted HTTPS are healthy. Rollback tag: `pre-recap-recovery-1c60f80`. |
