# Companion Space v0.1 Evidence Log

## Evidence Metadata

- Initial verification date: `2026-08-05`
- Latest evidence update: `2026-08-10`
- Evidence root: `docs/release/`
- Automated journey: `tests/e2e/mock-journey.spec.ts`
- Docker smoke script: `scripts/docker-smoke.sh`
- Status vocabulary:
  - `PASS`: the command or review completed successfully.
  - `WAITING`: the check could not start because a required local runtime is not ready.
  - `NOT RUN`: no valid execution evidence was collected.
  - `N/A`: intentionally outside the v0.1 release boundary.

The results below were observed in the local release session. This file records
the exact command and result summary; raw terminal transcripts are not committed
to the repository.

## Automated Release Checks

| Check | Command | Status | Observed result |
| --- | --- | --- | --- |
| Backend static analysis | `.venv/bin/ruff check services/api` | PASS | `All checks passed!` |
| Backend regression suite | `PYTHONPATH=services/api .venv/bin/pytest services/api/tests -q` | PASS | `268 passed, 7 warnings in 23.50s`. Backend coverage includes the read-only legacy import API, review scheduling, separated material/memory/review prompt context, vault, provider failures, realtime service/state transitions, artifact/recap generation, space isolation, activation/citation metric semantics, full-storage secret/audio canary scans, M6 topic boundaries, and fail-closed custom-provider DNS validation. |
| Web type checking | `npm run typecheck:web` | PASS | TypeScript completed with no errors. |
| Web lint | `npm run lint:web` | PASS | ESLint completed with zero warnings or errors. |
| Production web build | `npm run build:web` | PASS | Next.js `15.5.22` production build completed successfully, and the Docker-image rerun also passed. |
| Browser regression suite | `npm run test:e2e` | PASS | Playwright Chromium: `14 passed, 1 skipped (2.2m)` from an isolated non-iCloud checkout of the same worktree. The full-suite Mock journey completed in `1.1m` (a focused rerun was `54.3s`). Added browser evidence covers real WebSocket board updates, avatar `idle/listening/thinking/speaking` mapping, and visible/retryable 401, 429, and post-delta upstream-stream failures without silent Mock fallback. |
| Browser scheduled-playback latency | `npm run test:e2e -- tests/e2e/realtime-guardrails.spec.ts --project=chromium -g "records submit-to-scheduled-playback latency from the browser playback signal" --repeat-each=3` | PARTIAL | Earlier isolated verification persisted `546 ms`, `494 ms`, and `609 ms`, all below the `800 ms` gate. The 2026-08-10 Avatar Source Lifecycle verification kept the same threshold but measured `1118-1228 ms` in five software-rendered VRM runs. The matching backend first-binary test passed five times, while a WebGL-disabled browser diagnostic passed two of three runs and had one `1557 ms` cold-start outlier. The current Docker/SwiftShader lane is therefore not signed off as a fresh latency PASS. Web Audio exposes the scheduled source start, not a reliable hardware-audible callback, so this remains the browser proxy for first playback. |
| Hardware VRM performance | `npm run test:e2e:vrm-performance` | PASS | Headed Chromium: `1 passed (33.7s)`; the lane rejects SwiftShader/software renderers and requires sustained median FPS of at least 30. |
| Realtime production soak | `npm run test:e2e:realtime-soak` | PASS | `1 passed (30.2m)`; the soak scenario timing recorded in `soak-evidence.json` was `30.1m`, with 121 turn iterations and 61 samples. `residueFindings=[]`, `browserLeak=false`, `apiLeak=false`, `browserProcessLeak=false`, browser heap max delta was about `0.854 MiB`, and the API/browser-process RSS stayed below baseline. |
| Frontend dependency audit | `npm audit --audit-level=high --package-lock-only` | PASS | `found 0 vulnerabilities`. |
| Backend dependency audit | `PIP_DEFAULT_TIMEOUT=60 .venv/bin/pip-audit -r services/api/requirements.txt --progress-spinner off` | PASS | `No known vulnerabilities found` for the resolved runtime dependency set after pinning Starlette `1.3.1`. The stale local-only `python-multipart` package was removed rather than added to the runtime manifest because the application does not use form parsing. |
| Independent security review | In-session security-review agent | PASS | `APPROVE`, overall risk `LOW`; no critical, high, or medium finding. An existing fail-open unresolved-DNS SSRF edge was fixed test-first for non-Ollama custom endpoints. Provider URLs are also re-resolved immediately before adapter use; request-time IP pinning remains a low-risk hardening item under the local-owner threat model. |
| Compose and script preflight | `docker compose config --quiet` plus shell syntax checks | PASS | `docker compose config --quiet` passed; the smoke/E2E shell syntax checks were valid. |
| Docker Compose smoke | `COMPOSE_PROJECT_NAME=companion-space-smoke-codex-0805-final ./scripts/docker-smoke.sh` | PASS | Caddy, API, web, HTTPS app shell, health route, vault proxy, WebSocket ticket guard, and image-sensitive-path checks passed under the isolated project name; a follow-up `docker compose ... ps --all` returned no containers. Docker Engine `29.6.1` exposed the known invalid BuildKit session header, and the script's exact-error legacy-builder fallback completed both images. |

## Mock Journey Coverage

The passing Playwright suite used isolated runtime data under
`.playwright/mock-storage`. Its full-flow journey covered:

1. Initialize and unlock the local vault.
2. Discover the built-in Mock provider.
3. Create a blank study space and paste/index a Markdown note.
4. Create a character and bind it as the space default.
5. Start a text-first call, retrieve the note, show a server citation, and render
   board actions produced by the real turn/event path.
6. Run, pause, advance, and interrupt the three-step lesson demo.
7. End the session and wait for asynchronous recap artifacts.
8. Edit and persist the recap, restore the AI draft, confirm a memory candidate,
   and edit the generated review item.

The journey also asserts the required `2 / 4 / 6 / 4 / 6` body, face, hair, outfit,
and accessory option counts. The other default-suite scenarios verified the bundled
VRM/fallback boundary, realtime captions and interruption, microphone-denial
guidance, text fallback, the 250 ms local stop requirement, the persisted
submit-to-scheduled-playback metric, real WebSocket board updates, avatar state
mapping, and explicit provider failure/retry behavior. The 30-minute case is skipped
by the default suite and must be invoked explicitly.

The latest full-suite full-flow run in this refresh completed in `1.1m`; a focused
rerun of the same journey completed in `54.3s`.

The PCM injection lane validates the client-side realtime state machine and the
scheduled playback proxy, not real microphone capture. Physical microphone,
speaker, and headset validation remains `NOT RUN`.

Performance evidence is collected in a dedicated serial lane. An earlier latency run
that overlapped other final verification work recorded `844 ms`; it was treated as a
contaminated run rather than silently discarded. With competing build/browser jobs
removed, five consecutive local samples were `542 / 517 / 474 / 537 / 539 ms`, and the
independent three-run verification above was `546 / 494 / 609 ms`.

## Realtime Soak Evidence

The formal soak used a production `next build`/`next start`, one FastAPI WebSocket
session, and headless Chromium. Headless software WebGL intentionally exercised the
product's 2D avatar fallback; the separate headed hardware lane owns the VRM FPS
claim.

- Duration: 1,800,000 ms (`30.0` minutes); Playwright completed in `30.1m`.
- Workload: 121 turn iterations and 61 resource samples in one realtime session. The audio
  path included a real 640-byte PCM16 frame, Mock STT, streamed LLM output, Mock
  PCM16 TTS, and client interruption.
- Chromium JavaScript heap: 7.778 MiB baseline, 8.631 MiB final, 0.854 MiB maximum
  positive delta; fixed allowance 32 MiB.
- Chromium process RSS: 630.313 MiB baseline, 384.453 MiB final, no positive delta;
  fixed allowance 128 MiB.
- API RSS: 145.750 MiB baseline, 41.234 MiB final, no positive delta;
  fixed allowance 96 MiB.
- Residue scan: zero findings across files, SQLite, WAL, and logs. The scan checked
  audio extensions plus the exact uplink PCM canary, deterministic downlink Mock
  TTS bytes, RIFF/WAVE/audio/PCM markers, and transcript text in logs.

The generated `soak-evidence.json` lives under the ignored Playwright results
directory for each run. The stable observed values are recorded here because raw
runtime artifacts and logs are intentionally not committed.

`npm run build:web` and `npm run test:e2e` were verified sequentially in the same
worktree. The E2E web-server bootstrap intentionally clears `apps/web/.next`, so
those two commands must not run concurrently against one checkout. CI runs them in
isolated jobs.

One direct Desktop-worktree E2E attempt was discarded after macOS iCloud Drive
(`bird`) saturated CPU while syncing generated `.next` and Playwright artifacts;
page navigation took 15–18 minutes and even the 240-second timeout fired late. The
same source state was copied to `/private/tmp`, with the existing dependency
directories linked read-only, and completed the refreshed full suite in `2.2m`. This is an
environment-isolation rerun, not a retry that changes product behavior.

## Documentation And Scope Checks

| Check | Status | Evidence |
| --- | --- | --- |
| Neutral product naming and configurable display name | PASS | `README.md`, `.env.example`, and the built web application use `Companion Space` by default. |
| Mock-first path documented | PASS | `README.md`, `docs/release/v0.1-release.md`, and `docs/release/v0.1-runbook.md`. |
| BYOK setup documented without claiming provider validation | PASS | `docs/release/v0.1-release.md` and `docs/release/v0.1-runbook.md`. |
| `law-luze` remains optional and non-default | PASS | Pack is under `sample_data/demo-packs/law-luze/`; `test_fresh_container_starts_blank_without_law_luze_seed` passed in the backend suite. |
| `storage/` is runtime-only | PASS | `README.md`, `.gitignore`, and `docs/release/v0.1-runbook.md`. |
| Phone/LAN pairing is marked v0.2 | N/A | Explicitly outside v0.1 in the release notes and manual matrix. |
| Privacy and safety wording matches current defaults | PASS | Release notes and runbook document server-side keys, untrusted materials, and ephemeral raw audio. |
| Provider failures remain visible and do not silently change vendor | PASS | Release notes and runbook document the contract; Playwright exercised 401, 429, and a stream that emitted a valid delta before disconnecting, preserving the draft and retry path each time. |

## External And Manual Gates

| Gate | Status | Reason |
| --- | --- | --- |
| Real OpenAI-compatible, Anthropic, Gemini, and ElevenLabs credentials | NOT RUN | No real provider credentials were supplied to this release session. |
| Ollama runtime | PASS | Companion API used an isolated vault and space to test a local Ollama `0.32.5` connection, discover `deepseek-r1:1.5b`, bind `chat_llm`, and receive a streamed final `CompanionTurn`. Temporary storage was removed and contained no plaintext master password. |
| Physical microphone, speakers, and Bluetooth headset | NOT RUN | The automated PCM injection path only exercises the client state machine and scheduled playback proxy; it does not constitute hardware validation. |
| Desktop Edge | NOT RUN | No Edge manual session was executed. |
| Bundled licensed VRM on hardware WebGL | PASS | The headed hardware lane loaded the bundled VRM, rejected software rendering, and passed the sustained 30 FPS gate. |
| Malformed VRM assets | PASS | An isolated product-API upload rejected a corrupt `.vrm` body with HTTP 400 and an actionable format/signature error; the browser guardrail separately proved that VRM load failure keeps captions, interruption, session ending, and text conversation usable. |
| WebGL unavailable fallback | PASS | A browser test forced every `webgl`, `webgl2`, and `experimental-webgl` canvas context to return null, then verified the explicit WebGL error, 2D portrait, WebSocket connection, and text reply. |
| Dedicated Session Review reference-image comparison | N/A | No reference image is defined for v0.1. Functional behavior and accessible labels passed Playwright, but no visual-fidelity score is claimed. |
| Phone/LAN pairing | N/A | Deferred to v0.2 by scope. |

See [v0.1 Manual Matrix](v0.1-manual-matrix.md) for the scenario-level record.

## Release Verdict

**LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF.**

The current release session rechecked the automated gates recorded above. Final
v0.1 release sign-off still requires the remote-provider and physical-device rows
currently marked `NOT RUN`.

Phone/LAN pairing is not a missing v0.1 gate; it is a v0.2 feature.

## Windows Handoff Evidence — 2026-08-06

This section appends Windows execution evidence; it does not revise earlier macOS or release-session measurements.

### Mock Product Journey

- Docker Desktop `4.85.0` / Engine `29.6.2` ran an isolated, fresh Companion stack at `https://companion.localhost`.
- The browser initialized and unlocked an isolated Vault, created a space, indexed a note, created and bound a character, received a server-generated citation, played a three-step demo, asked a question during playback, ended the session, edited and saved the recap, confirmed a memory candidate, and edited/rescheduled a review item.
- The final review prompt and answer persisted. Browser logs contained no business errors.
- The isolated Storage contained SQLite, encrypted Vault, and the expected Markdown material only. It contained no plaintext test password and no audio files. The exact labeled test volume was inspected and then permanently removed; production Storage was never mounted into that journey.

### Portable Material Storage Hardening

- New database values use `spaces/{space_id}/materials/{material_id}.{ext}` relative to `OBJECT_STORAGE_PATH`, always with POSIX separators.
- Repository startup performs the legacy rebase in one `BEGIN IMMEDIATE` transaction before the ingestion worker starts. Only a path whose final four segments exactly match the material identity and suffix is rewritten.
- The shared resolver rejects unknown roots, relative traversal, wrong space/material identity, and final or intermediate symlinks. Ingestion, deletion, retry, and legacy-import idempotence use the same resolver.
- Automated evidence: full Ruff plus `284 passed`; focused Ruff plus `30 passed` after the final cross-platform, rollback, and symlink review changes.
- A black-box Docker rehearsal copied production Storage into an isolated labeled volume. The old API image created a real absolute `/app/storage/...` material; the candidate migrated it to the exact relative key, preserved exact content, reported zero changes on a second migration, passed SQLite integrity, and served `/healthz`. The test container and volume were then removed.

### Production Deployment

- Before rollout, `C:\Companion-Space\storage` was stopped and copied to `C:\Companion-Space\backups\pre-relative-path-20260806-173503\storage`; all seven files and `212720` bytes matched by SHA-256.
- The previous API image remains tagged `companion-space-api:pre-relative-path-ebdf693`.
- The running API image is `sha256:0fdcda3531cb504ac31b029ba4cb2ed6534fe8e3d7f1f6ce7b6eb38ae7ed7f04`.
- API, web, and Caddy are running. HTTPS `/healthz` returned `{"ok":true}`; the web shell and Vault proxy loaded; production SQLite returned `integrity_check=ok` and contained no non-portable material paths.
- The authorized Caddy root remains in the Windows current-user trusted root store with thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`. The in-app browser loaded the Vault as initialized and locked with no console warnings or errors.

The release verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote-provider credentials, physical microphone/speaker/Bluetooth hardware, and Desktop Edge have still not been supplied or executed.

### SQLite Lifecycle And Recoverable Backup — 2026-08-06

- Commit `c778bd3` records schema version 1 with `PRAGMA user_version`, rejects a future version before persistent mutation, runs v0→v1 under one `BEGIN IMMEDIATE` transaction, rechecks the version while holding the write lock, and applies an explicit configurable `busy_timeout` before lock-taking operations.
- TDD evidence began with five expected failures: fresh databases remained at version 0, future version 2 was accepted, a synthetic late migration failure left partial schema changes, the configured timeout was ignored, and the legacy FTS fixture stayed unversioned. After implementation, the final full backend gate passed Ruff plus `297 passed`; the focused lifecycle/maintenance lane passed `13 passed`.
- The online backup path uses `sqlite3.Connection.backup()` and validates `integrity_check`, `foreign_key_check`, schema version, size, and SHA-256 before publication. Tests prove committed WAL inclusion, uncommitted-row exclusion, busy checkpoint reporting, no overwrite, future-schema capture without initialization, and linked-destination rejection.
- Windows PowerShell 5.1 AST parsing, Compose configuration, TypeScript, ESLint, and the Next `15.5.22` production build passed. Real Windows execution exposed and fixed both parameter-time `$PSScriptRoot` handling and native stderr behavior before formal rollout.
- Isolated Windows rehearsals passed both `-OnlineDatabaseOnly` and the default full mode. The SQLite-only manifest explicitly recorded `complete_application_backup=false`; full mode stopped and restored the same API container, required a non-busy `TRUNCATE` checkpoint, and published a verified SHA-256 manifest.
- Before production migration, the default full script published `C:\Companion-Space\backups\companion-space-full-20260806-184705-b795eba2`: seven files, `220912` bytes, schema version 0, `integrity_check=ok`, zero foreign-key violations, and zero manifest hash failures. The old API resumed on image `sha256:0fdcda3531cb504ac31b029ba4cb2ed6534fe8e3d7f1f6ce7b6eb38ae7ed7f04` before the cutover and remains tagged `companion-space-api:pre-sqlite-c778bd3`.
- Production now runs API image `sha256:b18ede0b9f49ff338d06fb83f2ab11a9c1ff612d7cd33e2ff8bcf1728f75b49c`. API, web, and Caddy are up; HTTPS `/healthz` returned `{"ok":true}`; Vault returned initialized/locked; SQLite reports schema version 1, WAL, `integrity_check=ok`, and zero foreign-key violations.
- Logical data preservation was checked rather than inferred: all 15 business-table names matched between the pre-upgrade backup and production, all 127 rows had identical per-table counts, and no table count differed.
- Recovery was rehearsed on a copy, not on production. The pre-upgrade full backup was copied to `backups/restore-rehearsal-c778bd3-20260806-1849`; the new image migrated that copy to schema version 1 with WAL, clean integrity, and zero foreign-key violations. Its 15 business tables and 127 rows matched the pristine backup, while all six non-database files matched the manifest by SHA-256.
- The authorized Caddy root thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` remains present in both the Windows current-user and local-machine trusted root stores.

### Learning-Artifact Restart Recovery — 2026-08-08

- Commit `41817ef` makes FastAPI lifespan scan SQLite for learning-artifact sessions left in `pending` or `running`. Open sessions recover summary-only work; ended sessions recover summary, candidate-memory, and review generation. Rows already `ready` remain untouched, and repeated startup on one process is idempotent.
- Generation is limited to one active attempt. A `VaultLockedError` returns the affected session to `pending`, releases the generation slot so runnable Mock or other jobs can proceed, polls outside the slot, and retries after unlock. Graceful shutdown cancels in-memory tasks without converting recoverable states to false errors. The deployment remains explicitly single-process until a database claim/lease exists.
- TDD began with five expected failures for the missing repository scan, startup method, Vault behavior, idempotent shutdown, and lifespan wiring. The final focused recovery lane passed Ruff plus `11 passed`; it covers a real fresh `ServiceContainer` through `TestClient(app)` lifespan, mixed locked/runnable jobs, uninitialized Vault initialization, cancellation while polling, visible provider failure, serial concurrency, stale candidate/pending replacement, and preservation of confirmed/completed user decisions. Final full backend evidence is Ruff plus `308 passed` with seven known deprecation warnings.
- TypeScript, ESLint, Next `15.5.22` production build, Docker Compose configuration, and `git diff --check` passed. Independent final review found no remaining medium, high, or critical issue.
- An isolated Windows Docker rehearsal used one open `pending` session and one ended `running` session. The exact production API image `sha256:30477ac621132add81a1bb3789eea27c784e6735df56f9d2aa372387273ef709` recovered both to `ready`, kept the ended session's manual summary, returned `{"ok":true}`, and left SQLite at `integrity_check=ok` with zero foreign-key violations. Both labeled synthetic containers were gracefully stopped with exit code 0 and removed; production Storage was never mounted into the rehearsal.
- Before rollout, the verified full backup `C:\Companion-Space\backups\companion-space-full-20260808-234445-d74853ba` captured seven files, schema version 1, `integrity_check=ok`, zero foreign-key violations, and a complete SHA-256 manifest. The prior API image remains tagged `companion-space-api:pre-recap-recovery-1c60f80`.
- Production now runs API image `sha256:30477ac621132add81a1bb3789eea27c784e6735df56f9d2aa372387273ef709`; API, web, and Caddy are running with restart count 0. Trusted HTTPS `/healthz` returned `{"ok":true}`, the web root returned 200, and the Vault proxy returned initialized/locked.
- Production contains no remaining `pending/running` recap job. All 14 non-metric business-table counts and all 81 row-content hashes match the pre-deploy backup exactly. The only post-backup row is the expected local `api_request` metric for this deployment check of `/api/v1/vault/status` with status 200.
- The authorized Caddy root thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` remains present in both Windows trusted-root stores.

### Native Windows CI And LF Gate — 2026-08-09

- `.gitattributes` now fixes every tracked `.sh` and `.ps1` file to LF. `scripts/windows-smoke.ps1` checks the declared Git attribute, rejects any CR byte, and parses every PowerShell script with Windows PowerShell 5.1.
- TDD began with the smoke failing on the missing LF contract. The final native smoke reported `8 LF files; 3 PowerShell files parsed`.
- The first native full-backend run exposed two test-fixture portability defects before production code ran: a directory symlink required Windows privilege, and the legacy-import symlink fixture did not handle the same boundary. The maintenance test now creates a directory junction on Windows, preserving real reparse-point rejection coverage without administrator privilege. The file-symlink-only test skips specifically on Windows error 1314 when Developer Mode is unavailable.
- Final native Windows evidence used Python `3.12.13`: Ruff returned `All checks passed!`; Pytest returned `305 passed, 3 skipped, 7 warnings in 48.28s`. The focused maintenance/import rerun returned `13 passed, 1 skipped`.
- `.github/workflows/ci.yml` now defines `windows-smoke` on `windows-latest` with Python `3.13`, Windows PowerShell 5.1 shell/LF validation, Ruff, and the full backend suite. The workflow parsed locally and the configured job fields were inspected.
- Commit `9190de4` was fast-forwarded into `C:\Companion-Space` without rebuilding or restarting Docker. Its first formal smoke correctly rejected stale CRLF bytes in the pre-existing checkout because adding `.gitattributes` does not rewrite existing worktree files. Exactly the 8 reviewed shell files were normalized to LF; the formal smoke then passed and both repositories were clean at the same commit.
- Post-sync runtime checks remained green: API, Caddy, and web were running; trusted HTTPS `/healthz` returned `{"ok":true}`; the web root returned 200; and authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` was present once in each of the Windows current-user and local-machine trusted-root stores.
- `NOT RUN`: no GitHub-hosted run URL exists because this session did not push the local commit. The native local PASS is not presented as a hosted Actions result. The overall release verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical audio hardware, and Desktop Edge remain open gates.

### Four-State VRMA Release Evidence — 2026-08-09

This evidence set covers the repository-owned `idle`, `listening`, `thinking`, and
`speaking` motions under
`apps/web/public/assets/characters/motions/`. The verified implementation is
commit `a84c18a`, deployed to the formal Windows Docker stack on 2026-08-09.

| Check | Status | Observed result |
| --- | --- | --- |
| Asset provenance | PASS | All four `companion-*.vrma` files are generated by `scripts/generate-cc0-vrma.mjs`, declared by the project as CC0 1.0 Universal, and indexed with SHA-256 values in the bundled manifest. No third-party motion asset is included. |
| Deterministic generator | PASS | `node scripts/generate-cc0-vrma.mjs --check` completed successfully and confirmed that the four VRMA files and manifest match deterministic generator output. |
| Khronos glTF validation | PASS | Khronos `gltf-validator` `2.0.0-dev.3.10` reported `errors=0` and `warnings=0` for each of the four VRMA files. |
| VRM runtime compatibility | PASS | `node scripts/verify-cc0-vrma-runtime.mjs` loaded and retargeted the four motions against both a VRM 1.0 model and a VRM 0.x model: eight exact model/motion combinations. Every generated clip had five tracks, zero unresolved property bindings, a running action, and an actual change to the normalized hips quaternion. |
| Backend regression | PASS | Full backend suite: `350 passed`; Ruff: `All checks passed!`. |
| Frontend regression | PASS | TypeScript, ESLint, and the Next.js production build all completed successfully. |
| Browser inventory | PASS | Playwright `--list` discovered 18 tests. |
| Isolated real-browser inspection | PASS | The `Stage Companion` preset exercised all four avatar states with `mode=vrma`, `running=true`, and `weight=1.000`. Motion time advanced and the rendered skeleton changed in every state. |
| Official browser automation | PASS | Playwright `1.62.1` completed `5 passed (55.4s)`. Coverage included the complete Mock journey, the bundled VRM path, reduced-motion behavior, rejection of an untrusted external motion URL, and a manifest-authorized motion loaded through a temporary Blob URL. The HTML report is `D:\Codex\Companion-Space-playwright-20260809\playwright-report\index.html`. |
| Formal Docker deployment of this VRMA candidate | PASS | Commit `a84c18a` was fast-forwarded into `C:\Companion-Space` after the verified full backup `C:\Companion-Space\backups\companion-space-full-20260809-202510-6224e5e5` reported `integrity_check=ok`, zero foreign-key violations, and a complete SHA-256 manifest. API image `sha256:c7f46e42e9ca76c2bf148e02427fbb588ad4aae2eee4aaeae46bb79f3dd15084` and web image `sha256:7de7bb53662ad4ac76fc5536bfa52c111087b79d9bdd413e3fc13fca338b47b3` are running with Caddy, each at zero restarts; the prior images remain tagged `pre-vrma-1887675`. An OpenSSL client trusted the Caddy root and verified TLS 1.3 plus HTTP 200 for `/healthz`, `/`, and `/api/v1/vault/status`; the deployed idle VRMA was 3616 bytes with SHA-256 `0ed3bb51dfe023eb650bc20e9810ab6299845133a461c5bd8e753e5aab31b59e`. Post-rollout SQLite remained at schema version 1 with `integrity_check=ok`, zero foreign-key violations, unchanged business counts, and only one expected additional `api_request` metric from verification; all six non-database Storage files still matched the backup hashes. Authorized root thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` remains present once in each Windows trusted-root store. |

### Provider Request-Time IP Pinning And PDF Security — 2026-08-09

- Commit `986447e` closes the remaining DNS-rebinding window for HTTPX-based providers. Each operation resolves and validates the endpoint inside the connection deadline, keeps one immutable address snapshot, connects only to validated IPs, and preserves the original hostname for TLS certificate verification, SNI, and `Host`. Redirects, environment proxies, origin overrides, and hostname fallback fail closed. Ollama alone keeps its explicit private/loopback exception while metadata, link-local, site-local, shared-address, reserved, multicast, and unspecified targets remain blocked.
- Multi-address connection uses address-family interleaving with a chained 250 ms Happy-Eyeballs stagger, immediate release after a failed predecessor, one total connect deadline, and explicit loser-stream cleanup. Tests cover a black-holed first address, a four-address incremental stagger with three black holes and a trailing success, immediate failure acceleration, exact IPv6/IPv4 ordering, simultaneous-success winner/loser closure, DNS timeout/cancellation, transient DNS failure as HTTP 503, blocked targets as HTTP 424, Host/SNI mismatch, redirect rejection, and explicit Mock transports bypassing production DNS.
- The direct HTTPcore contract is pinned at `1.0.9`. `pypdf` was upgraded from `6.14.2` to `6.15.0`, which fixes [GHSA-fwg2-594c-jp42](https://github.com/py-pdf/pypdf/security/advisories/GHSA-fwg2-594c-jp42) and [GHSA-fp3f-mc75-235c](https://github.com/py-pdf/pypdf/security/advisories/GHSA-fp3f-mc75-235c). The focused PDF ingestion lane passed `13 passed`.
- Final candidate evidence used the exact production dependency set: Ruff returned `All checks passed!`; the full backend suite returned `378 passed, 8 warnings`; `pip-audit -r services/api/requirements.txt` returned `No known vulnerabilities found`; Docker Compose configuration passed; and two independent final reviews reported no remaining medium-or-higher issue.
- Before rollout, the verified full backup `C:\Companion-Space\backups\companion-space-full-20260809-222434-44c545a5` recorded full mode, seven files, schema version 1, `integrity_check=ok`, zero foreign-key violations, and a complete SHA-256 manifest. The previous API image remains tagged `companion-space-api:pre-provider-pinning-f0763a9` at `sha256:c7f46e42e9ca76c2bf148e02427fbb588ad4aae2eee4aaeae46bb79f3dd15084`.
- Production now runs API image `sha256:e96f212cace54bd8812b8d2bb455efd2ca407ec2307ce2b3eadeb887a73d267e` with `httpcore 1.0.9` and `pypdf 6.15.0`. API, web, and Caddy are running with restart count 0; web and Caddy retained their previous images. The API bind remains exactly `C:\Companion-Space\storage -> /app/storage`.
- Trusted HTTPS `/healthz` returned `{"ok":true}`, the web root returned HTTP 200, and `/api/v1/vault/status` returned `initialized=true` and `unlocked=false`. Post-rollout online SQLite verification reported schema version 1, `integrity_check=ok`, zero foreign-key violations, and SHA-256 `a42023e98af3de60667d2b0e3498f527db62baa270bfc37424a387519482e303`; all six non-database Storage files still matched the pre-rollout backup hashes.
- Authorized Caddy root thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` remains present exactly once in both the Windows current-user and local-machine trusted-root stores.
- Real OpenAI-compatible, Anthropic, Gemini, and ElevenLabs credentials remain `NOT RUN`; this hardening evidence does not convert the remote-provider manual rows to PASS. The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF**.

### Character Card V2/V3 Persona Import — 2026-08-09

- Commit `bbc9b56` adds standalone Character Card V2/V3 JSON import through the existing character-import route. Compatible V3 `3.x` versions are accepted while the actual source version is retained. Name, nickname, persona fields, greetings, aggregate text, and upload bytes are bounded; the API enforces the JSON byte limit before buffering the request, and the browser mirrors the same 1 MB preflight limit.
- Imported description, personality, scenario, first message, message examples, and alternate greetings become CharacterPack persona content. `system_prompt` and `post_history_instructions` are validated then discarded; raw override values, extensions, and remote asset URLs are neither persisted nor exported. Lone UTF-16 surrogates fail as a client error before SQLite persistence. Runtime prompt regression proves the mapped persona appears only inside the existing escaped untrusted-character boundary.
- Final automated evidence: focused Character Card/API/prompt regression `81 passed`; full backend `395 passed, 8 warnings`; Ruff `All checks passed!`; TypeScript, ESLint, and the Next `15.5.22` production build passed. Independent code review reported no remaining medium-or-higher issue. The isolated real Chromium Mock journey imported a canonical V3 JSON, verified persisted persona/scenario and prompt-secret absence, then completed the original avatar/call/recap/memory flow: `1 passed (1.1m)`. Report: `D:\Codex\Companion-Space-character-card-qa-1786287874942\artifacts\playwright-report\index.html`.
- Before rollout, the verified full backup `C:\Companion-Space\backups\companion-space-full-20260809-234250-aedfff7a` recorded seven files, schema version 1, `integrity_check=ok`, zero foreign-key violations, and a complete SHA-256 manifest. Previous images remain tagged `companion-space-api:pre-character-card-033d13e` (`sha256:e96f212cace54bd8812b8d2bb455efd2ca407ec2307ce2b3eadeb887a73d267e`) and `companion-space-web:pre-character-card-033d13e` (`sha256:7de7bb53662ad4ac76fc5536bfa52c111087b79d9bdd413e3fc13fca338b47b3`).
- Production runs API `sha256:fba9fd28f0b02490bb5554daf81cc73e58e9105f01fcdf8ec5737d49f7f7d9cc` and web `sha256:051640eaa1fe20a27b2af43e3269d41f09f4c77d0d1674a67723c70970897d2c` with the unchanged Caddy image. All three containers are running with restart count 0; the API bind remains exactly `C:\Companion-Space\storage -> /app/storage`.
- Trusted HTTPS `/healthz` returned `{"ok":true}`, the web root returned HTTP 200, and Vault status returned initialized/locked. Production SQLite remains WAL, schema version 1, `integrity_check=ok`, and zero foreign-key violations. All six non-database Storage files matched the deployment backup by SHA-256. Authorized root thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` is present exactly once in both Windows trusted-root stores.
- The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical audio hardware, and Desktop Edge remain external `NOT RUN` gates.

### Companion Emotion Performance Bridge — 2026-08-10

- Commit `d9d4684` adds one strict seven-value companion-emotion contract (`neutral`, `warm`, `cheerful`, `curious`, `focused`, `playful`, `concerned`) across the API model, realtime decoder, transcript state, call shell, and avatar runtime. New text, push-to-talk, VAD speech, interruption, connection, and disconnect boundaries reset the avatar to neutral; only a validated final assistant turn may restore its emotion. Push-to-talk is disabled until an in-flight text request has fully left its `finally` path, closing the text/audio overlap window.
- VRM expression selection is fail-closed. A semantic mood is applied only when the public expression metadata reports a non-binary expression with both mouth and blink overrides set to `none`; unsafe, missing, binary, `blend`, or `block` candidates become a no-op. The runtime reads the post-`vrm.update()` output weight, keeps `aa` lip sync and blink independent, and continues the active VRMA body animation. Reduced-motion mode keeps body and gaze static while retaining safe expression, blink, and real push-to-talk/TTS mouth evidence. The bundled Seed-san binary-only moods intentionally degrade to no semantic mood instead of suppressing blink or speech.
- Final automated evidence: full backend `395 passed, 8 warnings`; Ruff `All checks passed!`; TypeScript, ESLint, and the Next `15.5.22` production build passed. The isolated Chromium suite completed the Mock journey, reduced-motion lane, and normal realtime lane with `3 passed (1.3m)`; after the final text/PTT concurrency guard, the normal realtime lane completed twice with `2 passed (38.1s)`. The retained HTML report is `D:\Codex\Companion-Space-avatar-emotion-qa-20260810\artifacts\playwright-report\index.html`. Independent final architecture review reported `CLEAR` with no remaining medium-or-higher issue.
- Before rollout, `C:\Companion-Space\backups\companion-space-full-20260810-005326-524a5606` recorded full mode, seven files, schema version 1, `integrity_check=ok`, zero foreign-key violations, and a complete SHA-256 manifest; all seven manifest hashes were independently recomputed successfully. Deployment-predecessor images remain tagged `companion-space-api:pre-avatar-emotion-3fa78c5` (`sha256:fba9fd28f0b02490bb5554daf81cc73e58e9105f01fcdf8ec5737d49f7f7d9cc`) and `companion-space-web:pre-avatar-emotion-3fa78c5` (`sha256:051640eaa1fe20a27b2af43e3269d41f09f4c77d0d1674a67723c70970897d2c`).
- Production now runs API `sha256:5398e7d00458f5beb4d1adacb790feb5a031c2d77f66e9741959566d5a1b7687` and web `sha256:f9bbfdbe439b801daf0722be221b5ab9398ca6444f1afa079bea98f909246f98`; Caddy remains on `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. All three containers are running with restart count 0, and the API bind remains exactly `C:\Companion-Space\storage -> /app/storage`.
- HTTPS verification kept certificate-chain and hostname validation enabled and returned HTTP 200 for the web root, `{"ok":true}` for `/healthz`, and `initialized=true`, `unlocked=false` for `/api/v1/vault/status`. The local Caddy CA has no online revocation endpoint, so Windows curl used `--ssl-revoke-best-effort` rather than disabling certificate validation. The active Caddy root file and both trusted-root entries all match authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`, present exactly once in each Windows store.
- Post-rollout SQLite remains at schema version 1 with `integrity_check=ok` and zero foreign-key violations. All six non-database Storage files still match the deployment backup by SHA-256. The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain external `NOT RUN` gates.

### Import-to-Runtime Readiness — 2026-08-10

- Commit `0e17824` makes `.vrm`, CharacterPack `.zip`, and Character Card `.json` imports navigate directly to the persisted character detail. The detail separates manifest facts from browser runtime facts: original upload filename, license/author/source and redistribution fields, VRM version, declared versus successfully retargeted motion states, fail-closed safe-expression availability, recognized appearance slots, and material-color hits. A persisted import whose WebGL preview fails is reported as saved-but-unavailable instead of remaining in an indefinite loading state; motion-only packs and recipe-only cards retain accurate fallback wording.
- Runtime capabilities remain ephemeral and are never written into `asset_manifest` or SQLite. User-pack model and motion assets still require manifest authorization and load only through temporary Blob URLs; untrusted remote motion URLs remain blocked. The broad `tail` appearance alias was removed so model hair geometry such as `hair_tail` cannot be misreported as an accessory slot.
- Final automated evidence used the candidate production dependency set: Ruff returned `All checks passed!`; the full backend suite returned `395 passed, 8 warnings`; TypeScript, ESLint, and the Next `15.5.22` production build passed. The final isolated Chromium run completed the full Mock journey plus all realtime guardrails with `18 passed (2.2m)`. It covers raw VRM UI import and protected model fetch, VRM0 four-state retargeting, configured-versus-ready degradation, a declared pack motion whose asset download fails, actual Blob-backed motion and bone change, and persisted import fallback when WebGL is unavailable. The retained HTML report is `D:\Codex\Companion-Space-import-readiness-qa-20260810-015650\playwright-report-final\index.html`. Independent code and test reviews both reported `CLEAR` with no remaining medium-or-higher issue.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-021444-8edebf99` recorded seven files, schema version 1, `integrity_check=ok`, zero foreign-key violations, and a complete SHA-256 manifest. Deployment-predecessor images remain tagged `companion-space-api:pre-import-readiness-a34f867` (`sha256:5398e7d00458f5beb4d1adacb790feb5a031c2d77f66e9741959566d5a1b7687`) and `companion-space-web:pre-import-readiness-a34f867` (`sha256:f9bbfdbe439b801daf0722be221b5ab9398ca6444f1afa079bea98f909246f98`).
- Formal Docker images were rebuilt from commit `0e17824`: API `sha256:ab83f9fc39424b8f117c8df192abdbea8628d76c7d84b0a12cc53e48ce52fbc8` and web `sha256:ea41e9ea44a50968f49a2d362d9caa35aec55cc160dbd5f00d41f87b0dd93de8`; Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. During post-rollout verification all three containers were up with zero restarts, and the API bind remained exactly `C:\Companion-Space\storage -> /app/storage`.
- Windows Schannel verification kept hostname and certificate-chain validation enabled and used `--ssl-revoke-best-effort` only because the local Caddy CA has no online revocation endpoint. The web root returned HTTP 200, `/healthz` returned `{"ok":true}`, and `/api/v1/vault/status` returned initialized/locked. The active Caddy root file and both Windows trusted-root entries matched authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`.
- Post-rollout SQLite remained WAL, schema version 1, `integrity_check=ok`, and zero foreign-key violations. All six non-database Storage files matched the deployment backup by SHA-256, and the deployment's 10-minute API/Web/Caddy log scan contained zero error-like lines. After verification, the exact temporary E2E containers were removed, the formal Compose services were deliberately stopped, and Docker Desktop exited with zero remaining Docker Desktop processes. API and Caddy exited 0; the web container's exit 1 was the expected npm lifecycle result of the requested `SIGTERM`, confirmed by its final log.
- The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain external `NOT RUN` gates.

### Avatar Source Lifecycle - 2026-08-10

- Commit `667620d` adds authenticated `PUT` and `DELETE` avatar-source operations for an existing character. A replacement validates the new `.vrm` before touching the active asset layer, atomically swaps the local model/license assets, removes stale imported VRMA references, and preserves the character ID, persisted persona, relationship, voice settings, creation time, and space bindings. Removal returns the same character to its selected built-in model without deleting the character. Filesystem and SQLite failures restore the previous layer; update, duplicate, delete, export, asset reads, replace, and remove share one process-local lock under the documented single-Uvicorn-worker deployment boundary.
- The Character Editor exposes replace and restore controls with explicit confirmation that unsaved draft edits are discarded. Blob URLs switch only after the new protected asset loads; a successful server replacement followed by a browser asset-download failure keeps the previous preview visibly marked as `previous-preview` and never attributes its runtime capabilities to the new manifest.
- Automated evidence used the candidate production dependency set. The focused character service/API lane returned `83 passed`; the full backend suite returned `405 passed, 8 warnings`; Ruff `0.12.1` returned `All checks passed`. TypeScript, ESLint, and the Next `15.5.22` production build passed. The focused real-browser lifecycle test passed and proved same-ID replacement, exact protected model bytes, VRM 1.0 to VRM 0.x runtime change, invalid-replacement atomicity, persisted-new/preview-old provenance, removal, protected-asset 404, and built-in fallback. The reduced-motion guard was changed from arbitrary sleeps to two consecutive real telemetry batches and then passed `3/3`. Independent code review and final verification both reported `CLEAR` with no remaining medium-or-higher issue.
- The full browser run is not presented as all green: before the reduced-motion synchronization fix it reported `20 passed, 1 skipped, 2 failed`; the reduced-motion test subsequently passed three consecutive runs. The unchanged `800 ms` browser first-playback gate remained above budget in the current Docker software-rendering environment (`1118-1228 ms`, five runs). The equivalent backend first-binary gate passed five consecutive runs, and the WebGL-disabled diagnostic passed two of three runs with one cold-start outlier. No threshold was relaxed and this environment-sensitive performance lane remains explicitly unsigned.
- Before deployment, `D:\Codex\Companion-Space-backups\companion-space-full-20260810-033927-815a4b72` recorded full mode, seven files, `237296` bytes, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and a complete SHA-256 manifest. Previous images remain tagged `companion-space-api:pre-avatar-source-eef9bfc` (`sha256:ab83f9fc39424b8f117c8df192abdbea8628d76c7d84b0a12cc53e48ce52fbc8`) and `companion-space-web:pre-avatar-source-eef9bfc` (`sha256:ea41e9ea44a50968f49a2d362d9caa35aec55cc160dbd5f00d41f87b0dd93de8`).
- Formal Docker images were rebuilt from commit `667620d`: API `sha256:08a9c8713ff0405582982ca18275a1b0fb8ecabb6a183211b93c29f34f5c98a4` and web `sha256:f7c3594c1b35e21842d996c05ba12d6e06759c1da71a3a2bea360bd60d3b46a1`; Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. All three containers ran with zero restarts. The API bind was exactly `C:\Companion-Space\storage -> /app/storage`; trusted HTTPS returned HTTP 200 for the web root, `{"ok":true}` for `/healthz`, and initialized/locked Vault status. Unauthenticated requests to both new avatar endpoints returned 401. Post-rollout SQLite remained at schema version 1 with `integrity_check=ok` and zero foreign-key violations, all six non-database Storage files matched the deployment backup by SHA-256, and the deployment log scan contained no traceback, unhandled, panic, or fatal lines.
- The active Caddy root and both Windows trusted-root entries matched authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`, present exactly once in each trusted-root store. After verification, the formal Compose services were deliberately stopped and Docker Desktop exited with no remaining Docker Desktop processes. API and Caddy exited 0; the web container's exit 1 was the expected npm lifecycle result of the requested `SIGTERM`, confirmed by its final log.
- The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because the current Docker/SwiftShader first-playback gate, real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### Companion Launcher And Session Resume - 2026-08-10

- Commit `42ea62b` adds a one-click dashboard launcher. It resumes the most recently updated non-closed session across spaces, otherwise opens the most recent space, and creates a local starter space only when none exists. A session is still created lazily by the first text message or explicit realtime connection, so opening the launcher does not create empty session rows.
- Resume links hydrate the exact persisted session and transcript before enabling connection, quick prompts, or text submission. The client rejects malformed IDs, closed sessions, cross-space responses, transcript turns from a different session or space, invalid states, and missing or empty `character_pack_id` data. An explicit `null` character binding remains authoritative and does not silently adopt a space default added after the session began.
- Browser owner tokens remain memory-only. A hard reload therefore fails closed with `Owner session required`; after the owner unlocks the Vault again, client-side navigation through the dashboard restores the same session and transcript without a second session `POST`. The regression also proves that a follow-up message persists to the original session.
- Final candidate evidence: the Next `15.5.22` production Docker build passed; independent TypeScript and ESLint runs passed; and the isolated Chromium Mock journey completed `1 passed (1.5m)`. The journey begins from a deterministic blank dashboard and continues through the original material, character, realtime, recap, memory, and review flow. It additionally covers resume, reload/re-authentication, malformed/closed/cross-space links, and a missing character-binding field. The retained local HTML report was written under the ignored `playwright-report/launcher-qa-20260810-final-3/` directory. Independent final code review reported `APPROVE` with no remaining medium-or-higher issue.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-044907-659b2a7f` recorded seven files, `245488` bytes, `complete_application_backup=true`, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and a complete SHA-256 manifest.
- The verified implementation was cherry-picked into the formal repository as commit `7d80552`. The prior Web image remains tagged `companion-space-web:pre-launcher-712a354` at `sha256:f7c3594c1b35e21842d996c05ba12d6e06759c1da71a3a2bea360bd60d3b46a1`. The new formal Web image is `sha256:a2c45c2d9e76a7cd0f29259a77651fc5a525497466bc200aff929951e2d590b1`; API remained `sha256:08a9c8713ff0405582982ca18275a1b0fb8ecabb6a183211b93c29f34f5c98a4`, and Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`.
- During rollout verification all three formal containers ran with zero restarts. Windows Schannel kept hostname and certificate-chain validation enabled and returned HTTP 200 for the Web root, `{"ok":true}` for `/healthz`, and initialized/locked Vault status. The active Caddy root and both Windows trusted-root entries matched authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`, present exactly once in each trusted-root store.
- Post-rollout SQLite remained WAL with `user_version=1`, `integrity_check=ok`, and zero foreign-key violations. The API storage bind remained exactly `C:\Companion-Space\storage -> /app/storage`, all six non-database Storage files matched the deployment backup by SHA-256, and the deployment log scan contained zero traceback, unhandled, panic, or fatal lines.
- The exact Launcher QA containers, volumes, and temporary candidate image were removed after verification. The formal Compose services were deliberately stopped: API and Caddy exited 0; Web exited 1 only because the requested `SIGTERM` ended `next start`, as confirmed by its final npm lifecycle log. Docker Desktop itself was left running because shutting the global engine could interrupt unrelated workloads. The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because the current Docker/SwiftShader first-playback gate, real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### Model-Aware Stage Framing - 2026-08-10

- Development commit `19c0bac` replaces fixed VRM camera coordinates with model-aware framing. The runtime measures visible mesh geometry after load and after genuine appearance changes, uses humanoid landmarks when available, applies a shoulder-centered upper-body envelope for portrait framing, fits full visible bounds for full-body framing, and recalculates only camera math for framing or Canvas-resize changes. Imported generic-URL VRM 0.x models now receive the same orientation correction as bundled VRM 0.x assets. Invalid or unusable bounds fail closed to the prior legacy camera profile; no model scaling, schema change, dependency, or per-frame bounds traversal was added.
- Stable runtime telemetry records fit source, measured bounds, framed size, target, distance, horizontal/vertical occupancy, camera position/direction/FOV, and selected framing. The final browser coverage verifies bundled full-body framing, portrait/full-body differences, request-free framing changes, narrow-aspect refitting and restoration without model reload, plus imported VRM 1.0 to protected generic-URL VRM 0.x replacement. Independent architecture and code reviews both reported `CLEAR`/`APPROVE` with no remaining medium-or-higher issue.
- Fresh verification passed the Next `15.5.22` production build, a separate TypeScript check, and ESLint. Focused real Chromium coverage returned `2 passed (42.8s)`; the complete Mock journey returned `1 passed (1.4m)`. Visual QA retained `D:\Codex\Companion-Space-framing-qa-20260810\portrait.png` (SHA-256 `79044ee1214e2940ebe5f556b1e453691ca2665601c93be91749f0b165449039`) and `full-body.png` (SHA-256 `7d3fec89d1b88e79784b958dba33cbc9b7af53bf90126f8f0133377534a74213`). The inspected VRM 0.x portrait used distance `1.262` and vertical occupancy `0.926`; full body used distance `3.556` and vertical occupancy `0.847`, with the complete body retained.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-055034-ce562fe5` recorded seven files and `245488` bytes, `complete_application_backup=true`, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and manifest SHA-256 `11c3cd3491a5917b994dfe306714ad4bbfe730f69660a1e495c1d83d42f14fe3`.
- The implementation was cherry-picked into the formal repository as commit `4d47e03`; development and formal Git tree hashes matched exactly and both worktrees were clean before image construction. The previous Web image remains tagged `companion-space-web:pre-framing-f9a01fd` at `sha256:a2c45c2d9e76a7cd0f29259a77651fc5a525497466bc200aff929951e2d590b1`. The new formal Web image is `sha256:5aa946d5b501e537c395b46961b506aa23df81d497e2a5eff4cb5235d631b582`; API remained `sha256:08a9c8713ff0405582982ca18275a1b0fb8ecabb6a183211b93c29f34f5c98a4`, and Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`.
- During formal verification all three containers ran with zero restarts. Windows curl/Schannel kept hostname and certificate-chain validation enabled while disabling only online revocation lookup for the local CA; the Web root returned HTTP 200, `/healthz` returned `{"ok":true}`, and Vault returned initialized/locked. Authorized root thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` was present exactly once in each Windows trusted-root store.
- Post-rollout SQLite remained WAL with `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and a non-busy empty checkpoint. The API bind remained exactly `C:\Companion-Space\storage -> /app/storage`; all six non-database Storage files matched the deployment backup by SHA-256. The 259-line pre-stop API/Web/Caddy log scan contained zero error, exception, traceback, fatal, or panic lines.
- After verification the formal Compose services were deliberately stopped and zero project containers remained running. API and Caddy exited 0; Web exited 1 only because the requested stop delivered `SIGTERM` to `next start`, confirmed by its final npm lifecycle log. The two exact temporary QA images were permanently removed after proving they had no container references; the formal `latest` and rollback images remain. Docker Desktop was left running to avoid affecting unrelated workloads. The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because the Docker/SwiftShader first-playback gate, real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### Local Stage Background Presets - 2026-08-10

- Development commit `67393f5` adds the persisted `CharacterRecipe.stage_background` contract with the closed local enum `neutral/study/midnight`. Missing values in legacy SQLite rows, CharacterPack ZIPs, standalone Workshop JSON, and version-skew API responses resolve to `neutral`; unknown API values fail validation. `neutral` exactly preserves the previous loading and ready gradients, while the `Stage Companion` preset uses `portrait + midnight`.
- Backgrounds are CSS-only on the shared `AvatarRuntime` viewport, so the transparent 3D Canvas and CSS 2D fallback render the same choice. Changing the background does not replace the Canvas, leave `data-ready=true`, request a VRM, reload motions, or trigger model-bounds measurement. No remote URL, uploaded image, AIRI asset/code, dependency, database migration, or new storage surface was introduced.
- Automated evidence used the current production dependency set. The focused character service/API lane returned `87 passed, 5 warnings`; the full backend suite returned `409 passed, 9 warnings`; Ruff `0.12.1` returned `All checks passed!`. The Next `15.5.22` production build, a separate TypeScript check, and ESLint passed. The complete isolated Mock journey returned `1 passed (2.1m)` and covered legacy import, all three computed backgrounds, request-free/runtime-stable switching, export, persisted route remount, and call propagation. A focused realtime guardrail run returned `6 passed (33.7s)`; the exact WebGL-disabled Midnight fallback rerun returned `1 passed (7.2s)`. Independent code and test reviews reported no remaining medium-or-higher issue.
- Visual QA inspected the real VRM 0.x `Stage Companion` on Midnight in the in-app browser: black hair and the dark outfit retained a clear rim, face, bow, and silhouette. The WebGL-disabled 2D portrait, captions, and control cards remained readable against the same Midnight background. The temporary fallback screenshot was deleted after inspection.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-082027-dcde38bf` recorded seven files and `245488` bytes, `complete_application_backup=true`, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and manifest SHA-256 `9535990ea1bb6d4b6f42fa07ffd82f5a0ba42f5bc5a46f961e5f573ae82c9369`.
- The implementation was cherry-picked into the formal repository as commit `efec87a`; development and formal Git tree hashes matched exactly at `caacc44b8762f782b69f644df59f5bbb3d47d9ac`. Formal images were rebuilt as API `sha256:1b4427d158b00116affee09d111f9e43007e159456d15c5715c4ab8aba78da2f` and Web `sha256:094d5dc82a462238aba11468ed971ef50879823a4b426bb1e72ff86efd692f3d`; Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. Previous images remain tagged `companion-space-api:pre-stage-background-f81909d` and `companion-space-web:pre-stage-background-f81909d`.
- During formal verification all three containers ran with zero restarts. Windows Schannel kept hostname and certificate-chain validation enabled and returned HTTP 200 for the Web root, `{"ok":true}` for `/healthz`, and initialized/locked Vault status. The API bind remained exactly `C:\Companion-Space\storage -> /app/storage`; the active Caddy root and both trusted-root entries matched authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`, present exactly once in each store.
- Post-rollout SQLite remained WAL with `user_version=1`, `integrity_check=ok`, and zero foreign-key violations. Twenty non-metric table content hashes matched the pre-deploy backup; only the expected single `local_metric_events` row from the Vault verification was added. All six non-database Storage files matched the backup by SHA-256, and the 39-line pre-stop API/Web/Caddy log scan contained zero traceback, unhandled, exception, panic, or fatal lines.
- After verification the formal Compose services were deliberately stopped and zero project containers remained running. API and Caddy exited 0; Web exited 1 only because the requested stop delivered `SIGTERM` to `next start`, confirmed by its final npm lifecycle log. Exact temporary E2E containers, the dependency volume, the QA screenshot, and temporary candidate tags/images were removed; Docker Desktop was left running to avoid affecting unrelated workloads. The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because the Docker/SwiftShader first-playback gate, real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### AIRI v0.11.3 Persona-Only ZIP Import - 2026-08-10

- The compatibility boundary is pinned to AIRI v0.11.3 commit `dbf812488829a61cc2e95909e021b215704d066c` and its published `airi-card-import-export.ts` contract: root `manifest.json` plus `card.json`, container version `1`, `chara_card_v3`, and card version `3.0`. The test package is a clean-room synthetic fixture derived from that fixed source contract; it is not claimed to be an exporter-produced AIRI binary and contains no AIRI character, model, texture, motion, or preview asset.
- Import is deliberately persona-only. Official card text fields are bounded and mapped into the existing untrusted CharacterPack persona layer. Prompt overrides, extensions, schema-unknown fields, remote URLs, and credentials are not persisted or executed. A package may declare `live2d-zip`, `spine-zip`, or `vrm`, including an opaque safe path, but Companion Space only verifies that the declared member exists and is non-empty; model bytes are never read, written, exported, activated, or served. A card-only package with no `resources` remains valid and uses the built-in VRM.
- ZIP handling is fail-closed for encryption, symlinks, traversal, excessive entries/central-directory size, executable payloads, Win32 device names and alternate-data-stream syntax, trailing-dot/space aliases, case-fold and NFC/NFD collisions, and collisions introduced when the native CharacterPack `assets/` prefix is removed. Existing native CharacterPack behavior remains intact, including Unix directory entries and exact NFD paths; native manifests cannot forge trusted AIRI provenance.
- Fresh candidate verification used the final code without concurrent edits: focused character service/API tests returned `118 passed, 6 warnings`; the full backend suite returned `440 passed, 10 warnings`; Ruff returned `All checks passed!`. The Next `15.5.22` production build, a separate TypeScript check, and ESLint passed. The isolated real Chromium import journey returned `1 passed (40.2s)` and verified HTTP 201, persisted persona/scenario, secret absence, source provenance, exact declared-model asset HTTP 404, built-in VRM readiness after hard reload, and zero browser requests for the declared source model. Three independent backend, frontend, and test reviews reported no remaining medium-or-higher issue.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-110233-33eb7c73` recorded seven files and `245488` bytes, `complete_application_backup=true`, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, zero manifest hash mismatches, and manifest SHA-256 `f26fd4ea2439888227451058f3fcee50a1c522582b852d7bbe92a76bfad7f7e7`.
- Development commit `93daaf7` was cherry-picked into the formal repository as `518c361`; both repositories matched tree `18c3903dad011cc613c44ecd1ce2965f7a187d9e` and were clean before image construction. Previous images remain tagged `companion-space-api:pre-airi-zip-fbd5acb` (`sha256:1b4427d158b00116affee09d111f9e43007e159456d15c5715c4ab8aba78da2f`) and `companion-space-web:pre-airi-zip-fbd5acb` (`sha256:094d5dc82a462238aba11468ed971ef50879823a4b426bb1e72ff86efd692f3d`).
- Formal API and Web were rebuilt serially and deployed as `sha256:8ac6162aaa955850ef21aff12f2a78d7fefe2cfb1f4a769f5e4b8ae537dc6d5c` and `sha256:01cd5e6647c6f8448793e04cf0e0670ffa72a0a45c47b058132621efc9bf2ceb`; Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. During verification all three containers ran with restart count 0, and the API bind remained exactly `C:\Companion-Space\storage -> /app/storage`.
- Windows curl/Schannel kept certificate-chain and hostname validation enabled, using `--ssl-revoke-best-effort` only for the local CA's unavailable online revocation endpoint. The Web root returned HTTP 200, `/healthz` returned `{"ok":true}`, and Vault returned initialized/locked. The active Caddy root file, CurrentUser root store, and LocalMachine root store all matched authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`, with exactly one matching certificate in each store.
- Post-rollout production SQLite remained WAL with `user_version=1`, `integrity_check=ok`, and zero foreign-key violations. All 20 non-metric table hashes and all six non-database Storage file hashes matched the deployment backup. The 69-line API/Web/Caddy log scan contained zero traceback, unhandled, panic, fatal, or exception hits.
- After verification the formal Compose services were deliberately stopped; API and Caddy exited 0, while Web exited 1 only because the requested stop terminated `next start`. The exact `companion-space-web:airi-verify` temporary image was deleted after confirming it had no container references; formal `latest` and rollback images remain. With zero running containers, Docker Desktop was stopped through `docker desktop stop --timeout 120`; the official status command then reported Docker Desktop not running, the engine pipe was absent, and zero Docker Desktop/backend/build processes remained. The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### AIRI v0.11.3 Embedded VRM Import - 2026-08-10

- This slice keeps the clean-room compatibility target pinned to AIRI v0.11.3 commit `dbf812488829a61cc2e95909e021b215704d066c`. It supersedes only the previous persona-only treatment of a declared `resources.displayModel.format="vrm"`: Companion Space now reads that one declared ZIP member, validates it through the existing VRM 0.x/1.0 parser, and stores it as the canonical protected assets `model.vrm` and `licenses/vrm-meta.json`. Card-only, `live2d-zip`, and `spine-zip` packages remain persona-only and do not import their model payloads.
- AIRI's outer manifest is not treated as an asset-rights grant. License, author, source, redistribution, modification, attribution, and usage facts are derived from the embedded VRM metadata; conflicting outer claims are ignored. The UI explicitly states that format and metadata validation do not prove usage or redistribution authorization. No AIRI model, texture, motion, preview, or other third-party asset was copied into the repository.
- Import remains separate from activation. The new character is not assigned to any space automatically; the user may explicitly choose it for the next new session or set it as a space default. The AIRI display name is retained as provenance, while `source_filename` is the declared ZIP member's basename. Browser loading continues through the authenticated character-asset endpoint and a temporary Blob URL, with the existing 2D fallback and Blob revocation lifecycle.
- Fresh candidate verification used the final integrated code: focused character service/API tests returned `122 passed, 6 warnings`; the full backend suite returned `444 passed, 10 warnings`; Ruff `0.12.1` returned `All checks passed!`. A separate TypeScript check, ESLint, and the Next `15.5.22` production build passed. The complete isolated Chromium Mock journey returned `3 passed (3.2m)`, including the original fresh-clone journey, the unsupported Live2D persona-only path, and a real bundled CC0 VRM embedded in an AIRI ZIP. The VRM path proved the exact protected `model.vrm` HTTP 200 response and bytes, Blob-backed runtime source, VRM 0.x rendering instead of the default VRM 1.0 model, retained persona, no request to the AIRI source path or built-in fallback model, and unchanged space defaults. Independent code and test reviews both reported `CLEAR` with no remaining medium-or-higher issue.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-120343-ac322b29` recorded full mode, seven files, `245488` bytes, `complete_application_backup=true`, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and manifest SHA-256 `71c1a570ddd69e0df0d17444bb42b74ed29d2c90b7f567d7ce1f69cf4e32aa2c`.
- Development commit `277a3ad` was cherry-picked into the formal repository as `94e3e30`; both repositories matched feature tree `6dd78ae713343f74be9b049b619ffbcc11928c34` before the evidence-only follow-up. Previous images remain tagged `companion-space-api:pre-airi-vrm-36ab857` and `companion-space-web:pre-airi-vrm-36ab857`.
- Formal API and Web images were rebuilt serially and deployed as `sha256:05f4ecfcff1e45571de3ab4b49e7bee92391196d3b0a63ad1cc6a20b46a258f9` and `sha256:29ff6ed158ea3cd471caaaf882f363747ce3e9834a3dd756afbfd9a4ca30af60`; Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. During formal verification all three containers ran with restart count 0. The API bind was exactly `C:\Companion-Space\storage -> /app/storage`, and host/container hashes matched for the backend importer and both changed frontend components.
- Windows curl/Schannel kept hostname and certificate-chain validation enabled, using `--ssl-revoke-best-effort` only for the local CA's unavailable online revocation endpoint. The Web root contained `Companion Space`, `/healthz` returned `{"ok":true}`, and Vault returned initialized/locked. The active Caddy root file, CurrentUser root store, and LocalMachine root store matched the authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`, with exactly one matching certificate in each store.
- Post-rollout SQLite remained at `user_version=1` with `integrity_check=ok`, zero foreign-key violations, size `233472`, and SHA-256 `a3e5879e0e25e6ecc364783a1cd3655045e83a4a3f6ac7cde3e7e3829ad7d677`. The 41-line deployment log scan contained zero traceback, error, critical, panic, or fatal hits.
- After verification the formal Compose services were deliberately stopped. API and Caddy exited 0; Web exited 1 only because the requested stop delivered `SIGTERM` to `next start`, confirmed by the final npm lifecycle log. Temporary QA containers and the temporary E2E image were removed. Docker Desktop was then stopped through its official CLI; the engine pipe was absent and zero Docker Desktop/backend/build processes remained. The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### Session-Scoped Character Activation - 2026-08-10

- Development commit `1709244` adds a `本次会话角色` selector to the call stage. Selecting a character previews its existing protected VRM/VRMA assets but does not create a session and does not change the space default. The first text or realtime connection creates a session with the exact selected `character_pack_id`; after creation, the response and persisted session binding are authoritative and the selector stays locked until that session ends. A restored session keeps its recorded role even if the current space default or character catalog changes.
- The switch boundary is deliberately a new session rather than an in-place mutation. The integrated Chromium journey proves zero session POSTs during preview, an exact override on the first session, protected AIRI `model.vrm` bytes and VRM 0.x runtime, restored-session role fidelity, a distinct second session using the unchanged space/default role, and unchanged default-character mappings for every space.
- Full-journey verification exposed and fixed an independent learning-artifact finalization race. Commit `6d0e119` prevents an older turn-summary-only task from publishing `ready` after its session has ended; the final candidate job remains authoritative. A deterministic event-gated regression proves generation order `[False, True]`, final summary publication, and one memory plus one review candidate without weakening the existing idempotence guard.
- Fresh final evidence: the learning-artifact lane returned `21 passed`; the complete backend suite returned `445 passed, 10 warnings`; Ruff `0.12.1` returned `All checks passed!`. The Next `15.5.22` production build, an independent TypeScript check, and ESLint passed. The complete isolated Chromium file returned `3 passed (3.5m)`: fresh-clone Mock journey `2.0m`, persona-only AIRI ZIP `4.4s`, and session-scoped embedded AIRI VRM `1.4m`. The retained report is `D:\Codex\Companion-Space-session-character-qa-20260810-125539\proof\html\index.html`. Independent code review reported `CLEAR` with no medium-or-higher issue.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-133905-c3fdf043` recorded seven files and `245488` bytes, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and manifest SHA-256 `a80d1582178928036b340c9305529ba00570175adc91ba07cc17e13533a413dc`.
- Development commits `1709244` and `6d0e119` were cherry-picked into the formal repository as `474d82f` and `9508dfe`. Both repositories matched tree `4db81a6097f7c7d6d7bdfd5aa4d5a173bfd73e89` and were clean before construction. Previous images remain tagged `companion-space-api:pre-session-character-ca2e144` and `companion-space-web:pre-session-character-ca2e144`.
- Formal API and Web were rebuilt serially and deployed as `sha256:e5b86002d9d320b96b1baa7832fa5a1489d7e3f1c53a76a431c3f985670af414` and `sha256:3638b85ac49890ab23cedf6321172496d650f04f32930ad34b2a5baf8e99051f`; Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. All three ran with restart count 0, the API bind was exactly `C:\Companion-Space\storage -> /app/storage`, and host/container hashes matched for all five changed production files.
- Windows Schannel kept hostname and certificate-chain validation enabled and returned HTTP 200 for the Web root, `{"ok":true}` for `/healthz`, and initialized/locked Vault status. The active Caddy root, CurrentUser root store, and LocalMachine root store matched authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`, present exactly once in each store; no duplicate import was performed.
- Post-rollout SQLite remained WAL with `user_version=1`, `integrity_check=ok`, and zero foreign-key violations. All 20 non-metric table hashes and all six non-database Storage file hashes matched the deployment backup. The 36-line API/Web/Caddy log scan contained zero traceback, unhandled, panic, fatal, or exception hits. Exact QA containers, volumes, network, and seven temporary candidate/E2E image tags were removed; formal `latest` and rollback images remain.
- After final verification and explicit shutdown authorization, the formal API, Web, and Caddy services were deliberately stopped and `docker ps` returned no running containers. API and Caddy exited `0`; Web exited `1` only because the requested stop terminated `next start`, with `OOMKilled=false`, an empty container error, and restart count `0`. Docker Desktop was then stopped through `docker desktop stop --timeout 120`; its official status command reported that Docker Desktop was not running, the `docker_engine` pipe was absent, and zero Docker Desktop/backend/build/agent processes remained.
- The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### Atomic Space Default Character Activation - 2026-08-10

- Development commit `6ec7348` replaces the cached full-space update used by “set as default character” with owner-authenticated `PUT /api/v1/spaces/{space_id}/default-character`. Its strict body accepts only `character_pack_id: nonempty string | null`; repeated activation is an idempotent no-op whose `updated_at` remains unchanged. General space metadata updates can no longer carry a cached default-character value, closing the cross-tab lost-update path without a database migration or new dependency.
- Space-default activation, session creation, and character deletion now use SQLite `BEGIN IMMEDIATE` boundaries. Activation validates the referenced character and adult-relationship policy before changing only the target space. A new session atomically snapshots explicit character > current space default > built-in fallback, while existing session identity fields cannot be rebound by an upsert. Character deletion returns `409 character_in_use` while any space default or historical session snapshot still references it, preserving both database and protected-asset integrity.
- Fresh candidate evidence used the final integrated code: the focused activation/prompt lane returned `14 passed`; the complete backend suite returned `451 passed, 11 warnings`; Ruff `0.12.1` returned `All checks passed!`. The Next `15.5.22` production build, a separate TypeScript check, and ESLint passed. The isolated real Chromium Mock journey returned `3 passed (3.2m)`, including the full fresh-clone flow and the existing AIRI persona-only/embedded-VRM activation flows. Independent code and test reviews both reported `CLEAR` with no remaining medium-or-higher issue.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-145919-c8c5f218` published seven files with `complete_application_backup=true`, SQLite `integrity_check=ok`, zero foreign-key violations, and manifest SHA-256 `d6bc2ba6ca673de552caaac76698da44c6277db71cdc6f227c3615d0dbd255a7`.
- The development commit was cherry-picked into the formal repository as `d1912ab`; both repositories matched tree `0da2a24025bb244725bc214f5e4a6d91be8168c4` and were clean before image construction. Previous images remain tagged `companion-space-api:pre-space-activation-d1912ab` (`sha256:e5b86002d9d320b96b1baa7832fa5a1489d7e3f1c53a76a431c3f985670af414`) and `companion-space-web:pre-space-activation-d1912ab` (`sha256:3638b85ac49890ab23cedf6321172496d650f04f32930ad34b2a5baf8e99051f`).
- Formal API and Web were rebuilt serially and deployed as `sha256:59da53c0c46e4d81b7fc2ac6bf9c4f6fdfe1abfa65298c81698a91a8471e0a04` and `sha256:03917c9673758320bf3ebf7da8a1b34ae5f31b3bc2d2f11e836cdc13014f67fc`. The running container IDs matched those exact images; API, Web, and Caddy each stayed at restart count 0, and the recent log scan contained zero traceback, unhandled, panic, or error-level hits.
- Windows Schannel kept hostname and certificate-chain validation enabled; only unavailable local-CA revocation lookup was disabled. `/healthz` returned `{"ok":true}`, Vault returned initialized/locked, HTTP redirected to HTTPS with 308, OpenAPI exposed the exact new PUT operation, and a valid unauthenticated request returned 401 before state mutation. Authorized root thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` remained present exactly once in both Windows trusted-root stores.
- The live and backup databases both reported `integrity_check=ok` and zero foreign-key violations. Every non-metric table row count remained identical; the sole additional row was the expected local `api_request` metric for the deployment Vault probe. All six non-database Storage files still matched the backup manifest by SHA-256.
- After verification, the formal API, Web, and Caddy services were deliberately stopped and no container remained running. API and Caddy exited `0`; Web exited `1` only because the requested stop delivered `SIGTERM` to `next start`, with `OOMKilled=false`, an empty container error, and restart count `0`. The three temporary candidate/E2E image tags were removed. Docker Desktop was stopped through its official CLI; the engine pipe was absent and the Docker Desktop/backend/build/agent process count was zero.
- The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### Accessible Avatar Response Gesture — 2026-08-10

- Development commit `ee5472d` adds one clean-room, one-shot response gesture to the shared avatar runtime without adding an asset, dependency, API, database field, or persistent setting. The avatar viewport is a keyboard-focusable control named `让角色回应`; click, Enter, and Space each start one approximately 700 ms nod/light-lean response, key-repeat is ignored, and Space does not scroll the page. The same control works in Character Workshop and realtime calls. No AIRI code, model, motion, texture, or other third-party asset was copied.
- The gesture is additive after the current procedural or VRMA pose and never resets the active VRMA action. Previous offsets are removed before each new frame, stale sequence values do not replay after a model remount or reduced-motion change, and runtime/motion cleanup clears any applied offset. Actual post-`vrm.update()` head/spine quaternion deltas drive the gesture evidence rather than a self-reported target value. The 2D fallback supplies the same accessible acknowledgement and, under reduced motion, uses only a temporary non-motion highlight; the 3D runtime consumes activation while keeping body and gaze static.
- Fresh frontend verification used the final integrated files: the Next `15.5.22` production build passed without warnings, a separate TypeScript check passed, and ESLint with zero allowed warnings passed. Isolated Chromium completed the three new gesture tests with `3 passed (33.1s)` and three impacted VRM/reduced-motion/realtime regressions with `3 passed (37.9s)`. The retained reports are `D:\Codex\Companion-Space-avatar-gesture-qa-20260810\html\index.html` and `D:\Codex\Companion-Space-avatar-gesture-qa-20260810\regression-html\index.html`. Two independent final reviews reported `CLEAR` with no remaining medium-or-higher issue.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-173647-45e51293` recorded full mode, seven files, `complete_application_backup=true`, SQLite `integrity_check=ok`, zero foreign-key violations, and manifest SHA-256 `30000A3C26F12371635BD40D03DC3B389997978D67B468BA0F0BAC76ED9FC348`. The development commit was cherry-picked into the formal repository as `952009b`; both repositories matched feature tree `794813186ffd22bfa43158bd8d5b189443125199` before this evidence-only follow-up.
- The formal Web image was rebuilt and verified as `sha256:c9e9dad05599512b9582874533fa39ba621970e763839b1bb8e7a95e477d6946`. The previous Web image remains available as `companion-space-web:pre-avatar-gesture-952009b` at `sha256:03917c9673758320bf3ebf7da8a1b34ae5f31b3bc2d2f11e836cdc13014f67fc`. The unchanged API and Caddy images were `sha256:59da53c0c46e4d81b7fc2ac6bf9c4f6fdfe1abfa65298c81698a91a8471e0a04` and `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. During verification all three containers ran with restart count 0, no OOM or container error, and the recent log scan contained no traceback, fatal, panic, or error-level hit.
- Windows Schannel kept hostname and certificate-chain verification enabled while disabling only the unavailable local-CA revocation lookup. HTTPS `/healthz` returned `{"ok":true}`, the Web root returned HTTP 200, Vault returned `initialized=true` and `unlocked=false`, and HTTP redirected to HTTPS with 308. The live and backup databases both returned `integrity_check=ok` with zero foreign-key violations. Every table count matched except the expected deployment probe increment in `local_metric_events` (`113` live versus `112` backup); all six non-database Storage files matched the backup by path, size, and SHA-256, and neither tree contained a reparse point.
- After verification, the explicitly authorized formal API, Web, and Caddy shutdown completed and `docker ps` returned no running container. API and Caddy exited `0`; Web exited `1` only because the requested stop terminated `next start`, with `OOMKilled=false`, an empty container error, and restart count `0`. The exact unreferenced temporary image `companion-space-web:avatar-gesture-qa` (`sha256:2e931809707218aa9df99acec658e4aa2882370a81038e68b8c9d37b71cb393b`) was removed while the formal and rollback images were retained. Docker Desktop then stopped successfully through `docker desktop stop --timeout 120`; the `docker_engine` pipe was absent and the Docker Desktop/backend/build/agent process count was zero.
- The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### Managed Per-State VRMA Overlays — 2026-08-10

- Character Workshop now manages one Owner-uploaded local VRMA overlay for each exact runtime state: `idle`, `listening`, `thinking`, and `speaking`. `PUT` and `DELETE /api/v1/characters/{character_id}/motions/{state}` update only the trusted overlay layer; the persisted `CharacterRecipe` remains byte-for-byte equivalent. A removed overlay immediately reveals the recipe-provided motion or the existing procedural fallback.
- Uploaded bytes pass the existing strict VRMA GLB/humanoid/in-place validator and are stored as `managed-motions/{state}-{sha256}.vrma`. The server-generated `managed_motions` entry must match state, content-addressed path, source filename, Owner-upload provenance, local-only redistribution status, manifest `asset_paths`, and the file's current SHA-256. The reserved namespace cannot be forged by a native CharacterPack. Avatar replacement/removal and character duplication preserve valid overlays; filesystem/SQLite failures, concurrent uploads, aggregate-size limits, path collisions, and tampering fail closed without leaving staging state.
- Direct motion uploads carry no independently verified redistribution license, so a character with any managed overlay cannot be exported. No database migration, dependency, remote URL, catalog service, AIRI code, or third-party asset was added. The browser only receives authenticated protected-asset bytes and uses revocable `blob:` URLs; removing or replacing an overlay follows the existing stale-preview and Blob cleanup lifecycle.
- Fresh candidate verification used the final integrated files. The focused backend lane returned `27 passed, 123 deselected, 1 warning`; the complete backend suite returned `479 passed, 11 warnings`; Ruff `0.12.1` returned `All checks passed!`. The Next `15.5.22` production build, a separate TypeScript check, and ESLint with zero allowed warnings passed. Isolated Chromium completed the managed-overlay journey with `1 passed (23.7s)`, proving the exact protected asset request plus authenticated GET response bytes/MIME, real VRMA action time and skeleton movement, local-only export block, deletion 404, procedural fallback, idempotent second delete, unchanged recipe, and unchanged spaces/sessions. Two independent final reviews reported `CLEAR` with no medium-or-higher issue. Retained QA logs and the Playwright report are under `D:\Codex\Companion-Space-managed-vrma-qa-20260810-195807\artifacts`.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-204136-00a671a8` recorded full mode, seven files, `complete_application_backup=true`, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and manifest SHA-256 `80E5918EAFC3C040544C09D4B9B5EA5BCB5BA8DC0EF4C998D0BC0B5BA475E86D`. Development commit `9d0f1b9` was transferred through a hashed Git format-patch and applied to the independent formal repository as `f862205`; both repositories matched feature tree `9ad491675cf437fd64ce0aa762da737cdd5f78d6` before this evidence-only follow-up.
- Formal API and Web were rebuilt serially and verified as `sha256:2cd6d71ef942be1db0ec29b31c8726bc5591ed13bc4ed377f41d99baa3b7603c` and `sha256:d8de09bb547c1826b47506b0aaf3ef0d9fe95bfacc4c42302adb4fa340a31b02`; Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. Previous images remain tagged `companion-space-api:pre-managed-vrma-9490149` and `companion-space-web:pre-managed-vrma-9490149`. All seven changed production files matched the formal repository inside their running containers by SHA-256; API, Web, and Caddy each stayed at restart count 0 with no OOM or container error, and the API bind remained exactly `C:\Companion-Space\storage -> /app/storage`.
- Windows Schannel kept hostname and certificate-chain validation enabled while using best-effort revocation for the local CA. HTTPS `/healthz` returned `{"ok":true}`, the Web root returned 200, Vault returned `initialized=true` and `unlocked=false`, and HTTP redirected to HTTPS with 308. The deployed OpenAPI exposed both managed-motion operations and an unauthenticated DELETE returned 401. The active Caddy root and both Windows trusted-root stores matched authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`, present exactly once in each store.
- Live SQLite remained WAL with `user_version=1`, `integrity_check=ok`, and zero foreign-key violations. All 20 non-metric table content hashes matched the backup; three expected `local_metric_events` rows were added by the deployment HTTP probes. All six non-database Storage files matched the backup by path, size, and SHA-256, neither Storage tree contained a reparse point, and the 46-line final API/Web/Caddy log scan contained zero traceback, unhandled, panic, fatal, critical, or error-level hit.
- After verification, the explicitly authorized formal API, Web, and Caddy shutdown completed and `docker ps` returned no running container. API and Caddy exited 0; Web exited 1 only because the requested stop terminated `next start`, with restart count 0, `OOMKilled=false`, and an empty container error. The three exact unreferenced QA image tags were removed while formal and rollback images were retained; two isolated test Storage directories and the pip cache were deleted, while the roughly 2 MB evidence directory was retained. Docker Desktop then stopped through `docker desktop stop --timeout 120`; the engine pipe was absent and the Docker Desktop/backend/build/agent process count was zero.
- The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### Pointer And Touch Avatar Gaze — 2026-08-10

- Development commit `f939c1f` adds a clean-room AIRI-like gaze interaction to the existing avatar viewport without adding an API, database field, dependency, remote URL, model, motion, texture, or other asset. Primary mouse, pen, and touch input is normalized to `[-1, 1]` through a stable mutable ref; pointer leave/cancel, window or element blur, and non-mouse pointer up reset to center. The existing click/Enter/Space response gesture remains active on the same accessible control.
- In 3D, the input moves only the existing `VRMLookAt.target`. Evidence is sampled after `vrm.update()` from public `yaw/pitch`, so expression-based and bone-based LookAt models share one real-output contract. Pointer traffic does not trigger React state, replace the Canvas or scene root, request VRM/VRMA assets, or restart the active animation action. The CSS 2D fallback moves only the eyes. Reduced-motion mode consumes input but keeps the target, actual LookAt output, body, gaze sample, and fallback eyes static.
- Fresh frontend evidence used the final integrated files. The Next `15.5.22` production Docker build completed without warnings, a separate TypeScript check passed, and ESLint passed with zero allowed warnings. Isolated Chromium completed the three new mouse/touch/2D/reduced tests with `3 passed (15.6s)` and three impacted response-gesture/fallback/reduced regressions with `3 passed (19.0s)`. Retained reports are `D:\Codex\Companion-Space-pointer-gaze-qa-20260810\artifacts\new-html-pass\index.html` and `D:\Codex\Companion-Space-pointer-gaze-qa-20260810\artifacts\regression-html-pass\index.html`. Independent code, test, and verification reviews all returned `CLEAR` with no medium-or-higher issue.
- Before rollout, the verified full backup `D:\Codex\Companion-Space-backups\companion-space-full-20260810-224957-b94e49b8` recorded full mode, seven files and `249584` bytes, `complete_application_backup=true`, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and manifest SHA-256 `89B106218A70095D6F04C5F5429C3A6D1A48B994FAA4CC1178D82E03D23C9320`. The feature patch was retained with SHA-256 `AE004C4870C3E0E0527E8E4545691E15D951099CEAC5D80746D671455BC5D3E5` and applied to the independent formal repository as commit `f18a105`; both repositories matched tree `a9269b8363aecf5bbcb52c7197d37bfa08d5e1e4` before this evidence-only follow-up.
- The formal Web image was rebuilt serially as `sha256:fa730fed830357ed1b5311582a65b36de59be19d5d93dab296f6c2e1348cedea`; API and Caddy remained `sha256:2cd6d71ef942be1db0ec29b31c8726bc5591ed13bc4ed377f41d99baa3b7603c` and `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. The previous Web image remains tagged `companion-space-web:pre-pointer-gaze-f18a105` at `sha256:d8de09bb547c1826b47506b0aaf3ef0d9fe95bfacc4c42302adb4fa340a31b02`. All three changed production files matched the running Web container by SHA-256; API, Web, and Caddy stayed at restart count 0 with no OOM or container error.
- Windows Schannel kept hostname and certificate-chain verification enabled while using best-effort revocation only for the local CA. HTTPS `/healthz` returned `{"ok":true}`, the Web root returned 200, Vault returned `initialized=true` and `unlocked=false`, and HTTP redirected to HTTPS with 308. The active Caddy root and both Windows trusted-root stores matched authorized thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3`, present exactly once in each store. The API bind remained exactly `C:\Companion-Space\storage -> /app/storage`.
- Live SQLite remained WAL with `user_version=1`, `integrity_check=ok`, and zero foreign-key violations. All 20 non-metric table content hashes matched the backup; the sole additional row was the expected local metric from the deployment probe. All six non-database Storage files matched the backup by path, size, and SHA-256, neither tree contained a reparse point, and the 36-line API/Web/Caddy log scan contained zero traceback, unhandled, panic, fatal, critical, or error-pattern hit.
- After verification, the explicitly authorized formal API, Web, and Caddy shutdown completed and `docker ps` returned no running container. API and Caddy exited 0; Web exited 1 only because the requested stop terminated `next start`, with restart count 0, `OOMKilled=false`, and an empty container error. The exact unreferenced QA image `sha256:c290e5dfe100467ebc6a5f6b19ee2c7e7c849c37b0a4688bd288d7486bd222c9` was removed; the formal and rollback images remain. Docker Desktop then stopped through `docker desktop stop --timeout 120`; its status command reported that Docker Desktop was not running, the engine pipe was absent, and the Docker Desktop/backend/build/agent process count was zero.
- The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain unsigned or external `NOT RUN` gates.

### AIRI v0.11.3 Display Model Integration — 2026-08-11

- Development commit `b05edca` implements the fixed AIRI v0.11.3 Character Card ZIP contract for all three declared display-model formats: `vrm`, `live2d-zip`, and `spine-zip`. VRM continues through the validated Three/VRM runtime. Live2D and Spine archives are stored behind the Owner-authenticated asset endpoint with canonical paths, SHA-256, a generated local-only license record, and `validation_level=structure-only`; imported packages never auto-activate a space or session.
- Nested Live2D/Spine archives now have bounded member count, metadata size, expanded size, collision, traversal, symlink, encryption, executable, Unicode, and Windows-portability checks. Live2D model references, MOC3 headers, and PNG texture structure are checked; Spine requires a unique same-directory skeleton/atlas pair. Native CharacterPack re-import revalidates the server-generated manifest and license bytes, strips spoofed AIRI provenance, and cannot upgrade local-only redistribution rights.
- The browser uses a discriminated runtime-asset contract. A deployer-supplied same-origin bridge must implement `companion-avatar-runtime/v1`, expose an explicit first-frame `ready` promise, and own its renderer cleanup. SHA mismatch, missing/cross-origin bridge, bad protocol, failed creation, rejected asynchronous update/resize, character-asset failure, and session-resume failure all fail closed without silently rendering Nova, a built-in VRM, or the CSS portrait. React Strict Mode cancellation is checked before third-party `create`, every created handle is destroyed exactly once, and same-content archive reloads receive distinct runtime identities.
- No Live2D Cubism Core, Spine runtime, proprietary model, AIRI source code, or new runtime dependency is bundled. Formal `NEXT_PUBLIC_LIVE2D_RUNTIME_BRIDGE_URL` and `NEXT_PUBLIC_SPINE_RUNTIME_BRIDGE_URL` both remained empty, so the deployed default is an explicit blocked state until the operator supplies a separately licensed same-origin bridge. The clean-room protocol and deployment obligations are documented in `docs/licensed-avatar-runtime-bridge.md`.
- Fresh candidate verification used the final integrated files. Ruff passed; the complete backend suite returned `510 passed, 11 warnings (46.88s)`. TypeScript and ESLint passed, and the formal Next `15.5.22` production image compiled, linted, type-checked, and generated all routes successfully. Isolated Chromium returned `14 passed (79.4s)` for the focused AIRI bridge suite and `5 passed (64.8s)` for impacted AIRI VRM/WebGL/reduced-motion/text-call regressions. Retained reports are `D:\Codex\Companion-Space-evidence\airi-avatar-runtime-20260811-122700\index.html` (SHA-256 `CE72EAB62214757C8098CAF38E5751B1A804A0F89DC1B124D68C4722A4EE19D3`) and `D:\Codex\Companion-Space-evidence\airi-vrm-regression-20260811-123900\index.html` (SHA-256 `D3439FD3974975FD09AFEE03C8F6E41C942FCE799EE3EC83660A7A15041932E5`). Independent frontend and test reviews reported `CLEAR` with no remaining medium-or-higher issue.
- Before rollout, `D:\Codex\Companion-Space-backups\companion-space-full-20260811-124025-4beceefd` recorded full mode, seven files, `complete_application_backup=true`, SQLite `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and manifest SHA-256 `5DB1D68377A62D3234404892D8E8FEC9D55067F8906B7C52E3534336561678A8`. The feature was transferred into the independent formal repository as `a8d480b`; development and formal repositories matched tree `dfe2a2aac23914a640e5c5b1738abf10109f429f`.
- Formal API and Web were rebuilt as `sha256:0c2135f3a961d03a7c0f2391638e5fe84ca7deb2b66621f4d7e8cddf48326926` and `sha256:32536d147e971bbc90e1cb57fbc85db9cd855f61828d0a0ea78195c784f8764b`; both remain tagged with `airi-full-a8d480b`. Caddy remained `sha256:748016f285ed8c43a9ce6e3aed6d92d3009d90ca41157950880f40beaf3ff62b`. Nine changed production files matched the running containers by SHA-256, all three containers stayed at restart count 0 with no OOM or container error, and the API bind remained exactly `C:\Companion-Space\storage -> /app/storage`.
- Windows Schannel kept hostname and certificate-chain verification enabled while disabling only unavailable local-CA revocation lookup. HTTPS `/healthz` and the Web root returned 200, Vault returned `initialized=true` and `unlocked=false`, and HTTP redirected to HTTPS with 308. Authorized root thumbprint `C168566CF7F21B516750A3363AC5208184FC1DD3` remained present exactly once in both Windows trusted-root stores.
- Post-rollout online SQLite verification returned `user_version=1`, `integrity_check=ok`, zero foreign-key violations, and a 249856-byte verified snapshot. All six non-database Storage files still matched the deployment backup by path, size, and SHA-256; Storage contained zero reparse points. The 36-line final API/Web/Caddy log scan contained zero traceback, unhandled, panic, fatal, critical, or error-pattern hit.
- After verification, the explicitly authorized formal API, Web, and Caddy shutdown completed and `docker ps` returned no running container. API and Caddy exited 0; Web exited 1 only because the requested stop terminated `next start`, with restart count 0, `OOMKilled=false`, and an empty container error. Docker Desktop then stopped through `docker desktop stop --timeout 120`; its status command reported that Docker Desktop was not running, the engine pipe was absent, and the Docker Desktop/backend/build/agent process count was zero. The pre-deployment formal commit `5a33bbe` remains in Git and the verified full backup is the authoritative data rollback point.
- The overall verdict remains **LOCAL RELEASE CANDIDATE — NOT FULLY SIGNED OFF** because real licensed Live2D/Spine bridges, remote providers, physical microphone/speaker/Bluetooth hardware, Desktop Edge, and a GitHub-hosted CI run remain operator-supplied or external `NOT RUN` gates.
